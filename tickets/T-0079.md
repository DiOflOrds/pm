---
id: T-0079
titel: "Muessen Core-Rollen organisationsweit gleich BESETZT sein — oder nur gleich definiert? (Brief N-0045)"
typ: problem
prozess: man3
rolle: pl
sprint: 32
status: in_review
prio: hoch
blocked_by: []
repo: pm
reviewer: qm
geändert: 2026-08-22
geplant_sprint: 38
erstellt: 2026-08-21
---

## Herkunft — Brief `pm/N-0045`, 2026-08-21 06:48

> *„Rollen: alle rollen im CoreTeam sollen gleich sein: z.B. der Prob Role in Plattform
> ist auf Ollama, alle anderen Prob - Rollen sind CoWork. Die CoreRollen sind gleich, dh.
> alle müssen dann umgestellt werden"*

## Der Bestand, nachgezählt

Ollama-besetzt sind **zwei** Instanzen: `PROB@platform` und `MAIL-RED@team-mail`, beide
auf `gemma3:27b` (`process/roles/besetzungen.yaml`, bestätigt vom Preflight). Alle
übrigen Core-Instanzen laufen auf `cowork`. **Die Beobachtung des Auftraggebers stimmt.**

## ⚠⚠ Der Brief beschreibt eine Regel, die es im Modell nicht gibt

Konzept 04: jedes aktive Projekt hat implizit das volle Core Team — die **Definition**
einer Rolle ist organisationsweit eine. Die **Besetzung** ist je Instanz konfigurierbar,
und genau davon lebt `pm/D010` (der Ollama-Versuch läuft absichtlich an **einer** Stelle).

> **Zwei Lesarten, und sie führen zu verschiedenen Bauten:**
> **(1)** Nur die Rollen-**Definition** ist gleich, die Besetzung darf abweichen → heute
> ist alles richtig, und es fehlt lediglich eine **sichtbare Anzeige** der Abweichung.
> **(2)** Auch die **Besetzung** muss organisationsweit gleich sein → dann ist der
> Ollama-Versuch in `platform` ein Regelbruch und muss **beendet oder ausgeweitet**
> werden.

## ⚠ Warum das Ticket gesperrt ist und keinen Termin trägt

Lesart 2 hieße, `PROB` **überall** auf Ollama zu stellen. `pm/T-0077` (Frist 28.08.)
fragt den Auftraggeber gerade, ob der Ollama-Zweig überhaupt weiterlaufen soll —
Option **C** dort beendet ihn. **Diese Frage vorher zu bauen, hieße die andere
vorwegzunehmen.**

`blocked_by: [T-0077]`, **kein Termin**: ein Termin an einer Sperre wäre eine Zusage über
fremdes Handeln (`SWR-198`).

## Klassifikation

**Klasse B** — Besetzungen konfiguriert das PM-Team mit Eintrag im Decision-Log. **Kein**
neuer Rollen-Bauplan (das wäre Klasse A), sondern die Motorisierung bestehender Rollen.
⚠ Core-Instanzen werden **pausiert, nie gelöscht**.

## DoD

Beide Lesarten mit ihren Folgen dem PM vorgelegt · Entscheidung im Decision-Log (Klasse B)
· bei Lesart 1: die Abweichung wird **sichtbar** statt stillschweigend · Reviewer ≠ Autor.

## Sperre umgehängt — Sprint 33, `pm/D029` = C

Der Auftraggeber hat `pm/T-0084` am 2026-08-21 um **10:41** mit **Option C** beantwortet:
die vier Aufgaben, die auf das entschiedene `pm/T-0077` zeigten, verweisen ab jetzt auf
eine **offene** Aufgabe — **`platform/T-0060`**.

> **Damit ist die Sperre wieder ehrlich: sie zeigt auf etwas Offenes, an dem jemand
> arbeiten kann, statt auf einen Beschluss, der längst gefallen ist (`SWR-193`,
> `SWR-204`).**

⚠ Der Wartegrund hat sich nicht geändert — nur sein Verweis ist jetzt einer, den eine
Prüfung lesen kann.

---

## Entsperrt in Sprint 36 (2026-08-22)

`platform/T-0060` ist **done** — der echte Ollama-Lauf ist belegt:
`Gateway: status=ok provider=ollama`, Modell `ollama/gemma3:27b`, **2 Artefakte**
(`skills/sup9-problemmanagement/SKILL.md`, `templates/issues/problem.md`), 221,2 s,
Registereintrag vom 2026-08-21T20:59:26 in `platform/management/runs/run-registry.jsonl`.

Die Sperre ist im selben Lauf geraeumt, in dem ihr Hindernis wegfiel (`SWR-204`).
Termin: **Sprint 36**.

⚠ Der Nachweis sagt „ein Lauf gelingt", **nicht** „der Takt laeuft rund": von 10
Gateway-Zeilen ist **1** erfolgreich, und die letzten acht Preflight-Zeilen melden
`1 Befund(e)`. Das ist Gegenstand von `platform/T-0066` (nichts zu tun) und
`platform/T-0067` (leerer `provider`), nicht dieses Tickets.


## Termin nachgezogen: Sprint 37 (2026-08-22, Abschluss Sprint 36)

Nicht in Sprint 36 geschlossen. Der Grund steht im Abschluss von
`pm/management/sprint-aktuell.md`; der Termin steht **hier**, weil eine Verschiebung erst
verschoben ist, wenn das Ticket sie traegt (`L-2026-08-17ag`, `L-2026-08-21dp`).

⚠ Nachgezogen **vor** `--beende`, nicht danach: `sprint_vergangen` (SWR-112) vergleicht
gegen den laufenden Sprint und haette sonst jedes dieser Tickets als Befund gemeldet —
genau die Lawine, die den Schnelltakt dreimal in Folge gesperrt hat.


## Termin nachgezogen: Sprint 38 (2026-08-22, Abschluss Sprint 37)

Nicht in Sprint 37 geschlossen. Der Grund steht im Abschluss von
`pm/management/sprint-aktuell.md`; der Termin steht **hier**, weil eine Verschiebung erst
verschoben ist, wenn das Ticket sie traegt (`L-2026-08-17ag`, `L-2026-08-21dp`).

⚠⚠ **Und diesmal ist die Plantabelle im SELBEN Schritt mitgezogen worden.** Sprint 36 hat
seine Tickets vorbildlich nachgezogen und seine Plantabelle stehen lassen — daraus wurden
**12 `plan_drift`-Befunde**, die den Auto-Abschluss des Hosts stundenlang bei Schritt
[1/6] mit Exit 1 abbrechen liessen. **Ticket und Plan sind zwei Aussagen zu einer Frage;
wer nur eine nachzieht, tauscht einen Befund gegen den anderen** (`L-2026-08-22e`).

---

## ENTSCHIEDEN — Sprint 38 (2026-08-22), vierte Berührung

Nach der **Regel der vierten Berührung** wird beim vierten Termin entschieden, nicht
terminiert. Entschieden als **`pm/B061`** (Klasse B, Decision-Log): **Lesart 1** —
die Rollen-**Definition** ist organisationsweit eine, die **Besetzung** ist je Instanz
konfigurierbar. **Es wird nichts umgestellt; die Abweichung wird sichtbar gemacht.**

### ⚠⚠ Die Beobachtung des Auftraggebers war exakt richtig und exakt vollständig

| Messung (2026-08-22, `effektive_besetzungen`) | Wert |
|---|---|
| Instanzen gesamt | **86** |
| Rollen | **13** |
| Rollen mit **uneinheitlichem** Motor | **1** — `PROB` (7× cowork, 1× ollama) |
| Motorverteilung | cowork **84**, ollama **2** |

`MAIL-RED@team-mail` ist die zweite Ollama-Instanz und taucht bewusst **nicht** auf: sie
ist die einzige Instanz ihrer Rolle. **„Einzig" ist nicht „abweichend".**

> **Er hat die einzige uneinheitliche Rolle dieses Hauses gefunden, bevor es irgendein
> Werkzeug konnte. Was fehlte, war nicht die Gleichheit, sondern ihre Sichtbarkeit.**

### ⚠⚠ Die Zahl, die Lesart 2 gegenstandslos macht

| Messung | Wert |
|---|---|
| offene Tickets | **40** |
| davon `rolle: prob` | **2** (`platform/T-0069` `in_review`, `team-termine/T-0009`) |
| davon mit `aufgaben_typ` | **1** von 40 — und das ist `T-0069`, bereits `in_review` |

**Alle acht `PROB`-Instanzen auf Ollama zu stellen, gäbe dem Takt heute NULL zusätzliche
Tickets.** Der Engpass ist nicht die Vereinheitlichung, sondern der fehlende
`aufgaben_typ` (`SWR-214`, `platform/T-0066`). ⚠ Die Frage des Briefes und die Ursache
des Stillstands sind **zwei verschiedene Fragen** — Lesart 2 hätte ein Register geändert
und sonst nichts.

Lesart 2 rückwärts (Ollama beenden) widerspräche `pm/D010`, das den Versuch ausdrücklich
an **einer** Stelle führt.

### Gebaut (`SWR-220`)

`organisation.motor_abweichungen` + `preflight.motor_zeilen`: jede uneinheitliche Rolle
wird bei **jedem** Lauf namentlich gemeldet, **mit der Entscheidung, die sie erlaubt**,
und **ohne Befundzähler**. 9 Zusicherungen, **6 Mutationsproben, 6 rot**.

⚠ **Widerruflich und ohne Aufwand:** hält der Auftraggeber an Lesart 2 fest, ist die
Umstellung eine Zeile im Register je Instanz. Core-Instanzen werden dabei **pausiert,
nie gelöscht**.

### DoD

Beide Lesarten mit Folgen vorgelegt ✓ · Entscheidung im Decision-Log (`pm/B061`) ✓ ·
Abweichung sichtbar statt stillschweigend ✓ · Reviewer ≠ Autor — **offen** (`qm`).

Sprint 38: vierte Beruehrung, wird entschieden statt terminiert

pm/B061 verbucht, SWR-220 gebaut, 9 Zusicherungen, 6 Proben 6 rot
