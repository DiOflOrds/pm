---
id: T-0085
titel: "Die Projektnummer P13 ist ZWEIMAL vergeben — team-mail und projects/p13 tragen dieselbe Kennung"
typ: problem
prozess: man3
rolle: pl
sprint: 34
status: open
prio: mittel
blocked_by: []
repo: pm
reviewer: qm
geändert: 2026-08-21
geplant_sprint: 36
erstellt: 2026-08-21
---

## Herkunft — beim Vergeben der nächsten Nummer aufgefallen

Für `team-termine` war eine Projekt-Kennung zu wählen (`pm/T-0083`). Beim Nachsehen, welche
frei ist:

| Träger | Kennung | Beleg |
|---|---|---|
| `team-mail` | **p13** | `process/teams/registry.yaml`: `projekt: p13`, Name „P13 Mail (Mail-Zusammenfassung)", umgewidmet 2026-08-21 (`pm/T-0073`) |
| `projects/p13` | **p13** | Ordnername; Projektauftrag „Produkt-Architekturbilder", freigegeben `p13/D000` = G0a, 2026-08-21 |

**Dieselbe Nummer, zwei lebende Einheiten, am selben Tag.**

> **⚠⚠ Das ist die Familie von `SWR-197` und `SWR-203` eine Ebene höher: eine Kennung, die
> in zwei Vergabewegen unabhängig entsteht, ist nicht eindeutig — und niemand merkt es,
> solange nur einer der beiden Wege benutzt wird. Bei den D-IDs hat genau das 17
> mehrdeutige Zitate erzeugt.**

⚠ Der Unterschied zu den D-IDs: es gibt hier **keine Vergabefunktion**, die man reparieren
könnte. `projects/p13` bekam seine Nummer aus dem **Ordnernamen** (Pool-Knopf), `team-mail`
aus der **Registry** (Umwidmung). Zwei Wege, keine gemeinsame Stelle.

## Was in Sprint 34 getan wurde — und was ausdrücklich nicht

**Getan:** `team-termine` hat **p16** bekommen (`team-termine/B001`), nicht p13. Eine dritte
Vergabe wäre die bequeme Handlung gewesen.

**Nicht getan:** die bestehende Dopplung auflösen. Beide Kandidaten haben ein gutes
Argument, und die Wahl hat Folgen, die über eine Beschriftung hinausgehen:

* `projects/p13` — der **Ordnername** ist nach Hausregel die Identität und wird nie
  umbenannt (`SWR-175`, `p9/D003`).
* `team-mail` — trägt „P13" seit dem 15.08. in Berichten, in der Agenda und **in Texten, die
  der Auftraggeber gelesen hat**. Eine Umbenennung macht ältere Berichte falsch.

> **Eine Nummer im Fließtext eines Berichts kann man nicht zurückrufen. Beide Antworten
> machen etwas Vorhandenes falsch — das ist keine Reparatur, sondern eine Wahl.**

## Zu klären, bevor gebaut wird (erst zählen, dann entscheiden)

1. **Wie oft wird „P13" jeweils zitiert?** Getrennt nach den beiden Bedeutungen, gezählt
   und nicht geschätzt — die Zahl hat in diesem Haus schon fünfmal die Bauform verändert.
2. **Gibt es weitere Dopplungen?** p14/p15 sind vergeben (`team-dashboard`, `promt-team`);
   `projects/` trägt p10–p13. Die Frage ist, ob die **nächste** Nummer aus einer Stelle
   kommt oder wieder aus zweien.
3. **Braucht dieses Haus überhaupt eine Projekt**nummer**?** Der Ordnername ist die
   Identität, die Nummer eine Beschriftung. Eine Beschriftung, die kollidieren kann und die
   niemand braucht, ist billiger abzuschaffen als eindeutig zu machen.

## DoD

Zitate je Bedeutung gezählt · Entscheidung aus der Zählung abgeleitet (nicht aus
Bequemlichkeit) · genau **eine** Stelle vergibt künftig Projektkennungen, oder die
Kennung entfällt · Zusicherung gegen die nächste Dopplung · Reviewer ≠ Autor.

## Termin

Sprint 35, prio **mittel**. **Nullte Terminierung.** ⚠ Nicht `hoch`: die Dopplung tut
heute nichts weh — sie ist eine falsche Beschriftung, kein falscher Zugriff. Sie im
Vorbeigehen zu entscheiden wäre der teurere Fehler.

---

## Sprint 35 — DoD 1 und DoD 3 gemessen. Die Messung beantwortet Frage 3.

### DoD 1 — Zitate gezählt (ganzer Bestand, ohne `.git`)

| Bedeutung | Zitate |
|---|---|
| eindeutig `projects/p13` (Architekturbilder) | **40** |
| eindeutig `team-mail` (P13 Mail) | **30** |
| aus der Zeile heraus **nicht entscheidbar** | **96** |
| **gesamt** | **166** |

⚠ **Die dritte Zeile ist das eigentliche Ergebnis und keine Messschwäche.** Mehr als die
Hälfte aller „P13" im Haus sagt nicht, welches P13 gemeint ist — und das ist genau die
Familie von `SWR-197` (dort: 214 mehrdeutige D-Zitate aus 14 IDs).

### ⚠⚠ DoD 3 — gemessen, und die Antwort macht die Wahl gegenstandslos

> **Die Projektnummer hat KEINEN Leser.**

| Gemessen | Ergebnis |
|---|---|
| Zugriffe auf ein Feld `projekt` im Quelltext | **47** — und **alle 47** meinen den **Einheitennamen** (`team-mail`, `p0`), nie die p-Nummer |
| `projekt:`-Feld aus `process/teams/registry.yaml` gelesen von | **nichts** — weder Python noch JS |
| p-Nummern im generierten `organigramm.json` | **0** (`organigramm.py` liest `name`, nicht `projekt`) |
| `steckbrief.yaml` mit `projekt:`-Feld | **0** — `projects/p13` hat seine Nummer **nur** aus dem Ordnernamen |

> **⚠⚠ Es gibt keine Vergabefunktion, die man reparieren könnte — und es gibt auch nichts,
> das die Nummer benutzt. Zwei Wege führen in ein Feld, das niemand liest.**

Damit ist die Frage des Tickets („Braucht dieses Haus überhaupt eine Projektnummer?")
gemessen beantwortet: **nein, funktional nicht.** Der Ordnername ist die Identität
(`SWR-175`, `p9/D003`), die Nummer eine Beschriftung in Berichten.

### ⚠ Und genau deshalb wird sie in diesem Lauf NICHT geändert

Beide naheliegenden Handlungen sind schlechter als die Messung:

* **Umnummerieren** macht Berichte rückwirkend falsch, die der Auftraggeber gelesen hat —
  für eine Beschriftung ohne Leser.
* **Eine Kollisionsprüfung einbauen** würde beim ersten Lauf über die **bestehende**
  Dopplung stolpern. Ein Dauerbefund ohne Weg nach vorn ist wörtlich die Bauform, die
  `SWR-166` dieses Haus **83 abgebrochene Läufe** gekostet hat — und `SWR-211` hat in
  Sprint 34 gezeigt, dass die Ausnahmeliste als Ausweg ihre eigene Falle mitbringt.

### Termin

**Sprint 36**, prio **mittel**. **Erste Verschiebung**, Grund benennbar und keine Floskel:

> **Die DoD verlangt „genau eine Stelle vergibt künftig Projektkennungen, ODER die
> Kennung entfällt". Beides ist eine Änderung am Registry-Schema, und eine Schemaänderung
> am Sprintende ist genau die Reihenfolge, die diesem Haus schon einmal sämtliche
> board-check-Workflows rot gemacht hat.**

⚠ Was Sprint 36 **nicht** mehr tun muss: zählen. Die Zahlen stehen oben, und sie zeigen in
eine Richtung — **die Kennung entfällt** ist der billigere der beiden DoD-Ausgänge, weil
nichts sie liest.
