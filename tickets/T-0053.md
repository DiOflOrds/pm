---
id: T-0053
titel: "Frage: 21-mal open -> in_review in der Historie — verbietet UEBERGAENGE einen Weg, den die Organisation tatsächlich geht?"
typ: problem
prozess: sup9
rolle: pl
sprint: 0
status: in_review
prio: mittel
reviewer: qm
blocked_by: []
repo: pm
frist: 2026-08-24
geändert: 2026-08-17
geplant_sprint: 10
erstellt: 2026-08-17
---

## Herkunft

Aus `pm/T-0048` (SWR-118). Die dort gebaute Historienprüfung hat beim ersten Lauf **52**
unzulässige Statusübergänge im Altbestand gefunden. Die Verteilung ist der Anlass für
dieses Ticket:

| Übergang | Anzahl | Deutung |
|---|---|---|
| `open -> done` | 28 | Sprung über den ganzen Ablauf — eindeutig ein Fehler. |
| **`open -> in_review`** | **21** | ⚠ Sieht **nicht** nach Schludern aus. |
| `done -> open` | 2 | Wiedereröffnung am erlaubten Weg vorbei (`done -> in_progress`). |
| `in_progress -> done` | 1 | Review übersprungen. |

## Die Frage

`UEBERGAENGE["open"]` erlaubt `in_analysis`, `in_progress`, `blocked`, `rejected` — **nicht**
`in_review`. 21 Vorgänge in mindestens vier Repos sind trotzdem genau diesen Weg gegangen.

**Zwei Lesarten, und sie führen zu entgegengesetzten Korrekturen:**

* **(a) Die Tabelle hat recht.** Wer ohne `in_progress` in den Review geht, legt etwas zur
  Prüfung vor, an dem nachweislich niemand gearbeitet hat. Dann sind die 21 echte
  Verstöße und die Regel bleibt, wie sie ist.
* **(b) Die Tabelle kennt einen echten Ablauf nicht.** Bei einem Ticket, dessen ganze
  Arbeit im **Schreiben des Tickets** besteht — Analyse, Entwurf, CR-Text — gibt es
  keinen sinnvollen `in_progress`-Zustand. Dann verbietet die Tabelle einen Weg, den die
  Organisation aus gutem Grund geht, und **21 Vorgänge sind der Beleg dafür**.

⚠ **Das ist bewusst nicht in `pm/T-0048` entschieden worden.** Dort ging es darum, ob die
Prüfung überhaupt misst, was sie messen soll. Ob die **Regel** stimmt, die sie durchsetzt,
ist eine andere Frage, und beide im selben Lauf zu beantworten wäre B025 — zumal die
Antwort auf diese hier den Sinn der Zahl `ALTBESTAND_ERWARTET = 52` verändert.

## Warum es kein „einfach erlauben" ist

Wird `in_review` von `open` aus erlaubt, fällt die Zusicherung „vor dem Review hat jemand
gearbeitet" **für alle** Tickets, nicht nur für die reinen Schreibtickets. Ein zusätzlicher
Zustand oder eine an den Tickettyp gebundene Regel wären die Alternativen — beides
Änderungen am Statusmodell, also Playbook Kap. 5, also ein CR.

## Zu messen, bevor entschieden wird

1. **Welche Tickettypen** stehen hinter den 21? Sind es überwiegend `change-request` und
   `problem` (Lesart b) oder quer über alle Typen (Lesart a)?
2. **Wie lange** standen sie in `in_review`? Ein Review von Sekunden ist eine
   Statuskosmetik, ein Review über einen Lauf hinweg ist ein echter Vorgang.
3. **Gab es zu diesen Tickets eine Arbeitsleistung**, die nur nicht als `in_progress`
   verbucht war (Commits an anderen Dateien im selben Zeitraum)?

## Definition of Done

1. Die drei Messungen liegen vor und stehen im Ticket — **mit Zahlen**, nicht als Eindruck.
2. Entscheidung zwischen (a) und (b) mit Begründung; bei (b) ein CR am Statusmodell.
3. Bleibt es bei (a), wird das ausdrücklich als Entscheidung vermerkt — „wir haben nichts
   geändert" ist sonst von „wir haben nicht hingesehen" nicht zu unterscheiden.
4. `ALTBESTAND_ERWARTET` ist auf den Stand nach der Entscheidung nachgezogen, falls sich
   die Zählweise ändert.

---

## ⚠ Antwort (Sprint 10, gemessen): **Nein — die Regel bleibt.**

Die Frage lautete: *verbietet `UEBERGAENGE` einen Weg, den die Organisation tatsächlich
geht?* Die Zahl 21 legte das nahe. **Nach Datum aufgeschlüsselt zerfällt sie in drei
Ereignisse:**

| Wann | Fälle | Was war das |
|---|---|---|
| 2026-08-06 12:31 | **7** (p0, ein Commit) | vor `setze_status` — die Prüfung gab es noch nicht |
| 2026-08-07 08:32–09:28 | **13** (p1, acht Commits in 56 Minuten) | eine Session, die am Schreibpfad **vorbei** schrieb |
| 2026-08-15 07:42 | **1** (`p1/T-0021`) | Einzelfall, neun Tage später |

`setze_status` mit der Übergangsprüfung existiert seit **2026-08-07 07:16**
(`platform 19ac5b0`, T-0062) — also **eine Stunde vor** dem p1-Block.

**Kein Repo außer p0 und p1 kommt vor.** In `pm`, `platform`, `p2`–`p9`,
`team-dashboard`, `projects` gibt es diesen Übergang **kein einziges Mal** — obwohl dort
seither der weitaus größte Teil der Arbeit lief.

### Warum die 21 wie eine Praxis aussahen

> **Eine Zahl von Befunden zählt Artefakte, keine Entscheidungen.**

13 der 21 stammen aus **einer** Sitzung, die reihenweise Status nachzog; sie zählen 13
Tickets, nicht 13-mal eine Wahl. Nach `pm/T-0048` waren die 52 nach **Übergangstyp**
gruppiert — und diese Gruppierung erzeugte die Vermutung, hier liege ein Arbeitsweg vor.
Dieselben Daten nach **Zeitpunkt** gruppiert zeigen drei Ereignisse.

Das ist die Schwesterlehre zu **L-2026-08-17q**: dort hieß „uns sind zwei aufgefallen"
nicht „es waren zwei"; hier heißt „21 Fälle" nicht „21-mal so gearbeitet".

### Entscheidung (Klasse C — das Team darf sie treffen, Playbook Kap. 16)

**`UEBERGAENGE` bleibt unverändert.** `open -> in_review` bleibt unzulässig. Begründet:

1. Der Weg wird **nicht** gegangen — in neun Repos und zehn Sprints kein Fall.
2. `in_review` ohne `in_progress` überspringt die einzige Stelle, an der ein Ticket
   „jemand arbeitet daran" sagt. Der Prüfer bekäme etwas vorgelegt, für das niemand die
   Bearbeitung angezeigt hat.
3. Der einzige Fall nach dem Bulk-Ereignis (`p1/T-0021`) ist ein Vorbeischreiben am
   Schreibpfad, kein Beleg für einen fehlenden Weg — genau der Befund, den `pm/T-0048`
   bereits gezogen hat.

**Kein Bau, keine Änderung, keine neue Prüfung.** Der Altbestand bleibt gemeldet und
blockiert nicht (SWR-118); ab Stichtag blockiert jeder neue Fall — und das ist richtig,
weil es keinen legitimen neuen Fall gibt.

### Widerlegbar

Träte bis Sprint 20 ein `open -> in_review` **über den Schreibpfad** auf (also einer, den
`setze_status` durchließe, weil jemand die Regel ändert) oder häuften sich Vorbeischreiber
in verschiedenen Repos, wäre diese Entscheidung falsch und die Frage neu zu stellen.
