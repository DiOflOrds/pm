---
id: T-0030
titel: "CR (pm/N-0025): Offene Aufgaben terminieren — Fristen für Backlog-Tickets, echter Takt für wiederkehrende Aufgaben"
typ: change-request
prozess: man3
rolle: pl
sprint: 0
status: in_progress
prio: mittel
reviewer: qm
blocked_by: []
repo: pm
frist: 2026-08-16
geändert: 2026-08-16
erstellt: 2026-08-16
---

## Herkunft

Brief `pm/N-0025` (12:08): „offene aufgaben müssen erledigt werden, beim PM gibts welche, die
offen sind, werden aber nicht gemacht, diese müssen auch terminiert werden. Tag+sprint (jede
stunde gibts ein durchlauf). wiederkehrende aufgaben müssen auch terminiert werden, dann diese
erledigt werden (z.b. jeden tag, woche um 14 Uhr..)"

## Befund — der Vorwurf stimmt, konkret belegt

`pm/T-0025` (CR aus `team-mail/N-0002`, Rest-Umfang „Sofort-Knopf zeigt im Klartext, womit er
läuft") steht seit der 10:40-Session **offen** und ist seither in mindestens fünf
Routine-Sessions (11:00, 11:21, 11:45, 12:16, 14:05) nicht aufgegriffen worden — nicht, weil
niemand davon wusste (die Agenda nennt es seit der 10:23-Session explizit unter Punkt 0c),
sondern weil in jeder dieser Sessions andere Punkte (SUP.9-Befunde, Briefe, P10) Vorrang
hatten und niemand geprüft hat, ob der Vorrang noch stimmt. `pm/T-0003` (Takt „LeLe je
Sprint/Durchlauf konsolidieren") ist strukturell dasselbe Muster: ein Takt-Ticket, das
permanent `open` bleibt (per Definition), ohne dass sichtbar wäre, ob es in der jeweiligen
Session tatsächlich geprüft wurde oder nur formal existiert.

## Ursache

Zwei getrennte Lücken, die der Brief beide trifft:

1. **Backlog-Tickets (CR/Problem) haben kein Fristfeld.** Nur Decision-Requests (`frist` +
   `default`) und Takt-Tickets (`takt: je-session`) haben ein Zeitkonzept. Ein CR mit `prio:
   mittel` kann beliebig lange offen bleiben, ohne dass irgendein Werkzeug das meldet — die
   Priorität steuert nur die Reihenfolge *innerhalb* einer Session, nicht *ob* eine Session
   sich das Ticket überhaupt vornimmt.
2. **„Wiederkehrend" heißt bisher „bei jedem Sessionlauf geprüft", nicht „zu einer bestimmten
   Zeit".** `takt: je-session` ist an den Session-Takt gekoppelt (F14, aktuell alle ~15–30 Min,
   solange die App offen ist) — ein Wunsch wie „jeden Tag/jede Woche um 14 Uhr" ist damit
   grundsätzlich nicht ausdrückbar, unabhängig davon, ob eine Session gerade läuft.

## Warum kein Sofort-Bau in dieser Session

Ein Fristfeld für Backlog-Tickets und ein echter Uhrzeit-Takt sind zwei bewusste
Entwurfsentscheidungen mit Wirkung auf jedes künftige Ticket und auf die Session-Taktung
selbst (F14) — nicht auf eine Anzeige. Beides in einer Routine-Session nebenbei zu entwerfen,
ist genau das Risiko aus B025/B038: ein zu schnell gebautes Werkzeug, das Termine anzeigt, sie
aber nicht durchsetzt (was passiert mit einer gerissenen CR-Frist — Eskalation, Neu-Priorisierung,
nichts?), wäre schlimmer als der jetzige, ehrliche Zustand ohne Fristen.

## Umfang (Entwurf für die Design-Session)

- Optionales Feld `frist` auch für `typ: change-request`/`problem` (nicht nur `decision-request`),
  mit einer Regel, was bei Fristüberschreitung passiert (mindestens: sichtbare Markierung im
  Board/Cockpit — „überfällig", analog zur bestehenden Frist-Warnmail SWR-033/034).
- Klärung, ob „Tag+Sprint" (aus dem Brief) ein eigenes Feld neben `sprint` werden soll oder ob
  `frist` genügt.
- Für wiederkehrende Aufgaben: ein Takt-Konzept mit Uhrzeit (`takt: taeglich@14:00` o. Ä.), das
  unabhängig vom Session-Rhythmus eine fällige Ausführung erkennbar macht — Abgrenzung zu F14
  (Session-Takt 0 €) und den bestehenden `team-mail`-Takten (SWR-063/064/T-0001 in team-mail)
  vorher klären, damit keine dritte Taktlogik neben den beiden vorhandenen entsteht.
- **Sofortmaßnahme, unabhängig vom Design:** `pm/T-0025` steht als konkretes Beispiel jetzt oben
  auf der Agenda (Priorität hoch statt mittel) statt nur als Randnotiz in Punkt 0c.

## Verifikation

Kein Code in diesem Ticket — Entwurf für eine dafür vorgesehene Session. Kein neuer SWR.

## Stichprobe (Auftraggeber)

Keine — reines Planungsticket bis zur Umsetzung.

---

## Umsetzung Teil 1 (Routine-Session 2026-08-16 15:20, SWR-091, PM-Beschluss B044)

Die Session, die dieses Ticket verlangt hat, war diese: Briefkasten leer, keine entschiedene
Inbox-Entscheidung, `T-0030` oberster offener Punkt der Agenda. Es ein sechstes Mal liegen zu
lassen, wäre die Wiederholung genau des Befunds gewesen, den es beschreibt.

**Geliefert (SWR-091, P9-Fläche v1.7):**

- `frist` ist für **jeden** Tickettyp zulässig und wird für jeden geprüft. Bis hierher stand die
  Datumsprüfung im `decision-request`-Zweig — ein Tippfehler in der Frist eines CR fiel lautlos
  auf „keine Frist" zurück.
- Die Ampel-Regel (rot/gelb/grün/grau) liegt **einmal** in `board.frist_ampel` und wird von den
  offenen DRs und den Backlog-Fristen geteilt; sie stand vorher inline in `aggregation.cockpit`.
  Ein Test vergleicht alte Inline-Formel und neue Funktion einen ganzen Monat lang Tag für Tag.
- `board.ist_ueberfaellig` gilt nur für offene Tickets — eine gerissene Frist an einem erledigten
  Ticket ist Historie, kein Vorwurf.
- Die Cockpit-Kachel zeigt überfällige Tickets **vollständig und vor den Statuszahlen**, jedes mit
  Frist und „n Tage über", dazu einen Zähler „n ohne Frist" für unterminierte Backlog-Tickets
  (Takt-Tickets ausgenommen, sie tragen ihr Zeitkonzept in `takt`). Sie in die auf drei gekürzte
  Aufgabenliste zu legen, wäre die Unsichtbarkeit aus B038 in neuem Gewand gewesen.

**Nebenbefund, mitbehoben:** `DATUM_MUSTER` prüfte nur die Form. „2026-13-01" kam durch und hätte
über `frist_ampel` als **grau = keine Frist** gegolten — ein falsch terminiertes Ticket hätte wie
ein unterminiertes ausgesehen. Jetzt eine gemeinsame Prüfung `board.ist_datum` für `frist` und
`erstellt`, nicht zwei.

**Bewusst nicht angefasst:** das generierte `BOARD.md`. Eine neue Spalte ist eine Formatänderung,
und genau die hat am 16.08. früh alle board-check-Workflows rot gemacht (`pm/T-0013`); sie gehört
gebündelt, nicht nebenbei.

**Eskalationsregel — die offene Frage dieses Tickets, festgelegt in B044:** Ein überfälliges
Backlog-Ticket ist der erste Arbeitspunkt der nächsten Routine-Session nach dem Briefkasten.
Nimmt eine Session es trotzdem nicht auf, schreibt sie den Grund beim Ticket in die Agenda. Ab
dem zweiten übergangenen Mal geht ein Vermerk in den Briefkasten des Auftraggebers.

**Sofort angewandt statt nur gebaut:** Die offenen pm-Backlog-Tickets haben jetzt Termine
(`T-0028`: 2026-08-23, `T-0032`: 2026-08-19). Ein Fristfeld, das nur künftige Tickets nutzen,
hätte den Befund nicht berührt.

**Zahlen:** 11 neue Tests, Gesamtsuite **329** (vorher 318), Matrix **91 SWRs / 0 Lücken**,
Katalog- und Architektur-Gate grün. Gegenprobe gegen den Altstand geführt — drei der neuen Tests
scheitern dort nachweislich.

## Teil 2 — abgetrennt als `pm/T-0032`

Der Uhrzeit-Takt („jeden Tag um 14 Uhr") berührt F14 (Session-Takt, `p0/D027`) und die beiden
bestehenden Taktlogiken; ihn im selben Zug zu bauen hätte eine dritte erzeugt, ohne dass die
Abgrenzung geklärt ist. Er steht mit Entwurf und eigener **Frist (2026-08-19)** in `pm/T-0032` —
dem ersten Nutzer des Feldes, das dieses Ticket gebaut hat.

## Stichprobe (Auftraggeber)

Nach einem Serverneustart: Cockpit öffnen → die Kachel `pm` zeigt über den Statuszahlen keinen
roten Block (beide offenen CRs sind terminiert und noch nicht fällig) und in der Statuszeile
`n ohne Frist` für die Takt-freien Tickets ohne Termin. Einem beliebigen offenen Ticket im
Ticket-Editor eine Frist von gestern geben → die Kachel zeigt es sofort als überfällig mit
„1 Tag über". Eine unsinnige Frist („2026-13-01") wird beim Speichern mit Klartext abgelehnt.

Teil 1 umgesetzt: SWR-091 (Fristen fuer Backlog-Tickets, ueberfaellige Aufgaben im Cockpit), 11 neue Tests, Suite 329, Matrix 91/0.
