# Decision Log — Projektmanagement-Team (append-only, Klasse-B-Entscheidungen nach Playbook Kap. 16)

| ID | Datum | Entscheider | Entscheidung | Optionen | Begründung | Betroffene Artefakte |
|---|---|---|---|---|---|---|
| B000 | 2026-08-15 | PM-Team (Klasse B, via Session) | **Arbeitsgrundlage übernommen:** Playbook Kap. 15/16, Registry, Charter, SLAs aus team.yaml — Rahmen aus p0/D027 (Klasse A) unverändert | — | Gründungsvollzug P5-E4 | Charter, team.yaml, Registry |
| B001 | 2026-08-15 | PM-Team (Klasse B, via Session) | **Session-Agenda v1 beschlossen:** Reihenfolge Briefkasten aller Projekte → fällige pm-Takt-Tickets → aktives Projekt (P5) → Betriebs-Backlog; gilt bis Einspruch oder Änderung | — | Erste Agenda nach F14 (Session-Takt) | management/session-agenda.md, T-0001 |
| B002 | 2026-08-15 | PM-Team (Klasse B, via Session) | **Pilot-Vorgehen:** Gründung team-mail als Routine nach intake.md v2 unter PM-Aufsicht mit 2-Wochen-Pilotreview — statt eigenem Projektrepo „P6"; die Gründung selbst bleibt Klasse A (T-0004) | Routine vs. eigenes Projekt | Gründung ist seit P5 Standardprozess; ein Projektrepo würde nur Verwaltung doppeln; Einspruch jederzeit | T-0004, intake.md v2, Orgkonzept Kap. 4 |
| D000 | 2026-08-15 | Mensch (E. John, via Inbox) | **TG-a** | lt. T-0004 | — | T-0004 |
| B003 | 2026-08-15 | PM-Team (Klasse B, via Session) | **Werkzeug-Ablage Pilot:** mail_digest.py v0 liegt als team-lokales Skript in `team-mail/tools/` (Datennähe, kein Plattform-Baseline-Eingriff); Promotion zum Plattform-/Katalog-Produkt mit SWRs+Tests wird im 2-Wochen-Pilotreview entschieden (CR ans ASPICE-Team) | team-lokal vs. platform sofort | Pyramide: erst beweisen, dann formalisieren; platform-Code erfordert requirements-first | team-mail/tools/, Pilotreview |
| B004 | 2026-08-15 | PM-Team (Klasse B, via Session) | **CR „konfigurierbare Teams + HMI" zweigeteilt:** Sofortteil team-lokal umgesetzt (konfiguration.yaml + mail_digest v0.2: Zeitraum/Mehrkonto/Rechnungs-Abschnitt); Plattformteil (Mission-Control-Ansicht + Konfigurator + Mail-Zustellung) als Klasse-A-DR T-0005 (P7) vorgelegt | sofort alles vs. zweiteilen | Team-lokales ist Klasse C und sofort lieferbar; Plattform-Arbeit braucht Auftrag + requirements-first | konfiguration.yaml, mail_digest.py v0.2, T-0005 |
| B005 | 2026-08-15 | PM-Team (Klasse B, via Session) | **Betriebs-CR „Mail-Direktlinks im Digest" eingeplant und umgesetzt** (Brief team-mail/N-0001): Renderer-Links (SWR-060, p7/T-0014), mail_digest v0.3 mit Message-ID-Deep-Links, heutiger Digest nachgerüstet — keine Budgetwirkung, kein neues Projekt | CR klein vs. neues Projekt | Kleiner Umfang auf bestehender P7-Fläche; requirements-first eingehalten (SWR-060) | p7/T-0014, SWR-060, mail_digest v0.3, Digest 2026-08-15 |
| B006 | 2026-08-15 | PM-Team (Klasse B, via Session) | **K4-Realnachweis P7 registriert:** Erste Digest-Mail nach Konfigurator-Aktivierung zugestellt (Zustellvermerk im Digest, Idempotenz greift) — P7-Restpunkt geschlossen; **Ankunft vom Menschen bestätigt** („mail ist da") | — | Klasse-B-Stichprobe lt. Agenda | team-mail/digest/2026-08-15-digest.md, p7-Abschlussbericht K4 |
| B007 | 2026-08-15 | PM-Team (Klasse B, via Session) | **Brief N-0002 zweigeteilt:** Wunsch 1 (Neustart-Knopf) als Betriebs-CR sofort umgesetzt (p7/T-0015, SWR-061); Wunsch 2 (Ollama-Autopilot, Sofort-Zusammenfassung, Mehrfach-Takt) als **P8-Auftrag mit Klasse-A-G0** vorgelegt, weil er F14/D027 (Session-Takt) für team-mail revidiert | CR vs. Projekt je Wunsch | Kleiner Umfang sofort; Ausführungsmodell-Änderung braucht den Menschen | p7/T-0015, p8/T-0001, N-0002 |
| D001 | 2026-08-15 | Mensch (E. John, via Inbox) | **P7b** | lt. T-0005 | — | T-0005 |
| B008 | 2026-08-16 | PM-Team (Klasse B, via Session) | **Abnahme-Kommentare (N-0003/4/5) qualifiziert:** Modellwahl (T-0006) und KI-Hinweisfeld (T-0007) als kleine CRs für die nächste Session eingeplant; Org-Dashboard als Klasse-A-DR T-0008 (P9) vorgelegt | CR vs. Projekt je Brief | Kleines sofort einplanbar; Dashboard-Umbau braucht Auftrag | T-0006, T-0007, T-0008, N-0003/4/5 |
| D002 | 2026-08-16 | Mensch (E. John, via Inbox) | **P9a** | lt. T-0008 | — | T-0008 |
| D003 | 2026-08-16 | Mensch (E. John, via Session + N-0008) | **projects-Sammelrepo beschlossen:** neue Projekte ab P10 als Ordner in EINEM Repo `projects` — kein neues GitHub-Repo/PAT je Projekt mehr; Discovery-Unterstützung als SWR-070 in P9 Sprint 1; Bestandsrepos bleiben | Monorepo vs. Repo je Projekt | Wunsch des Auftraggebers: manuellen Aufwand eliminieren | projects/, SWR-070, intake.md v3 |
| B009 | 2026-08-16 | PM-Team (Klasse B, via Session) | **Briefe N-0007/8/9 qualifiziert:** CI-Flake als T-0010 behoben (SUP.9); projects-Monorepo als D003 vollzogen (Hülle steht, Rest P9-S1); Session-Automatik als Vorschlag beantwortet (tägliche geplante Cowork-Session, Einrichtung auf Zuruf — 15-Min-Takt bewusst nicht: Routine läuft bereits über Aufgabenplanung/Autopilot) | — | Briefkasten-zuerst-Routine | T-0010, D003, N-0007/8/9 |
| D004 | 2026-08-16 | Mensch (E. John, via Session) | **Routine-Session alle 30 Minuten eingerichtet** (Cowork Geplante Aufgabe "aspice-routine-session"): arbeitet selbständig Briefkasten + PM-Agenda ab; Klasse A bleibt Inbox, Push bleibt Wächter-Weg; Takt jederzeit änderbar | 30 Min vs. täglich | Wunsch aus N-0009; läuft, solange die Cowork-App offen ist | Cowork Scheduled Task, session-agenda.md |
| B010 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **CRs T-0006/T-0007 umgesetzt** (Modellwahl SWR-071, KI-Hinweisfeld SWR-072) als Betriebs-CRs auf der P8-Fläche — requirements-first (p8-E4), Tickets p8/T-0009/T-0010, 178 Tests, Matrix 72/0; **keine neue Projekt-Baseline** (p8-v1.0 bleibt Abnahmereferenz), kein Klasse-A-Vorgang (kein Budget, keine Außenwirkung) | eigenes Projekt vs. CR auf P8-Fläche | Kleiner Umfang auf vorhandener Fläche — gleiches Muster wie SWR-060/061 auf der P7-Fläche (B005/B007); Auftrag lag über D004 bei der Routine-Session | p8/T-0009, p8/T-0010, SWR-071/072, pm/T-0006, pm/T-0007 |
| B011 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **SLA-Stichprobe team-mail bestanden:** Tages-Digest 2026-08-16 vom Autopilot erzeugt (59 Mails, Ollama-Verdichtung) und **zugestellt** — Zustellvermerk am Dateiende vorhanden, Einmal-Zustellung greift; Takt-Ticket T-0001 für heute erfüllt | — | K4-Realnachweis lt. Agenda Punkt 2 (Profil `wiederkehrend`: SLA statt G4) | team-mail/digest/2026-08-16-tag-digest.md, pm/T-0001 |
| B012 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **P9-Abnahme verbucht:** D002/G4a aus der Inbox vollzogen (T-0004 done, Abschlussbericht, Baseline `p9-v1.0` auf p9 + platform) — neun Projekte abgeschlossen; die drei Cockpit-Stichproben bleiben als Betriebs-Nachweis beim Auftraggeber offen und werden nicht nachträglich als erledigt gezählt | — | Entscheidung des Menschen lag vor (Klasse A erledigt), Verbuchung ist Klasse C/B | p9/T-0004, p9-Abschlussbericht, PROJEKTSTATUS-UPDATE.md |
| B013 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Briefe N-0010/N-0011 qualifiziert und erledigt:** N-0010 (Selbst-Neustart bei neuem Code) als Betriebs-CR auf der P7-Fläche umgesetzt (SWR-073, p7/T-0016) — mit Marker-, Ruhe- und Entprell-Sicherung, damit ein handgestarteter Server nie von allein verschwindet; N-0011 (Status „in progress") als **bereits vorhanden** beantwortet — echter Befund war die fehlende Sichtbarkeit, deshalb Sichtbarkeitsregel im Playbook Kap. 5 ergänzt (in_progress beim Arbeitsbeginn setzen) | CR vs. Projekt; neuer Status vs. Sichtbarkeitsregel | Kleiner Umfang auf vorhandener Fläche, kein Budget/keine Außenwirkung; ein zweiter Status mit gleicher Bedeutung hätte das Modell verdoppelt statt geklärt | p7/T-0016, SWR-073, Playbook Kap. 5, N-0010, N-0011 |
| B014 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Brief N-0012 qualifiziert und umgesetzt:** Ticket-Feld `takt` mit festem Vokabular, Takt-Spalte + Zähler im Board, Markierung in Board-Karte, Ticket-Detail und Cockpit (SWR-074, p9/T-0006) — dauerhaft offene Takt-Aufgaben sind jetzt von liegengebliebenen Einzelaufgaben unterscheidbar; Feld optional, Bestand ohne Feld unverändert | eigenes Statusmodell für Takt-Tickets vs. optionales Feld | Ein eigener Status hätte den Workflow gedoppelt; der Takt ist eine Eigenschaft der Aufgabe, nicht ihres Fortschritts | p9/T-0006, SWR-074, board.py, aggregation.py, app.js, alle BOARD.md |
| B015 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Briefe N-0013/N-0014 zweigeteilt:** Sofortteil umgesetzt — erledigte Aufgaben älter als 1 Tag werden im Board ausgeblendet, per Schalter wieder einblendbar, BOARD.md bleibt vollständiges Archiv (SWR-075, p3-Fläche); der Rest (Mehrfach-Labels, Typ selbst setzen, offene Tickets editieren) als **Klasse-A-DR pm/T-0011 „P10 Aufgaben bearbeiten im HMI"** vorgelegt (Frist 23.08., Default P10a, Umsetzung als Ordner im Sammel-Repo projects nach D003) | alles sofort vs. zweiteilen | Der Anzeigefilter ist ein Handgriff ohne Architekturwirkung; ein Ticket-**Schreibpfad** neben der Skript-Route braucht Auftrag, ADR und Konfliktbehandlung gegen die parallel laufende Routine-Session | SWR-075, p3-Requirements v1.1, pm/T-0011, N-0013, N-0014 |
| D005 | 2026-08-16 | Mensch (E. John, via Inbox) | **P10a** | lt. T-0011 | — | T-0011 |
| D005 | 2026-08-16 | Mensch (E. John, via Inbox) | **P10a** — P10 „Aufgaben bearbeiten im HMI" beauftragt (Ticket-Editor, Mehrfach-Labels, Typ setzen) | lt. T-0011 | — | T-0011 |
| B016 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **P10-Intake terminiert und N-0015 eingeplant:** D005/P10a verbucht (T-0011 done); Umsetzung nach intake.md v3 als **Ordner `projects/p10`** (erster Vollzug von D003) — Intake ist erster Punkt der nächsten Routine-Session. Brief N-0015 (Kopfbereich nur aktive Projekte/Teams, klickbar) als CR pm/T-0012 eingeplant, **nicht** im Schnellschuss umgesetzt | sofort vs. nächste Session | Der Kopfbereich hängt an der Hash-Navigation (ADR-005) — Deep-Links und Handy-Ansicht dürfen nicht brechen; abgeschlossene Projekte bleiben unter „weitere" erreichbar | T-0011, T-0012, projects/p10, N-0015 |
| B017 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Brief N-0016 sofort umgesetzt:** Inbox-Reiter trägt die Zahl der wartenden Entscheidungen und ist hervorgehoben, Aktualisierung jede Minute ohne Neuladen, Ausfall des Abrufs bleibt folgenlos (SWR-076, p3-Fläche) | sofort vs. mit T-0012 bündeln | Reiner Anzeigezusatz ohne Berührung der Navigation — anders als T-0012 (Kopfbereich/Hash-Router) risikolos sofort lieferbar | SWR-076, p3-Requirements v1.2, N-0016 |
| B018 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **P10-Intake vollzogen** (Agenda-Punkt 0, Auftrag D005/P10a): Projekt liegt als Ordner **`projects/p10`** — erster Vollzug von D003, kein neues GitHub-Repo. Angelegt: Projektauftrag mit Abnahmekriterien und Abgrenzung, STK-020, **SWR-077–081 als `draft`**, Sprint-0-Plan mit Risiken, Decision-Log D000 (G0-Äquivalent aus pm/D005), board-check-Workflow für das Sammel-Repo (prüft jeden Projektordner). **G1-DR `projects/p10` T-0002 liegt in der Inbox** (Frist 23.08., Default G1a) mit zwei ausdrücklichen Rückfragen: „neues Projekt" als Label statt Ticket-Typ, und Anlegen/Löschen von Tickets bewusst außerhalb des Umfangs | eigenes Repo vs. Ordner im Sammel-Repo; SWRs sofort `reviewed` vs. `draft` bis G1 | D003 gilt ab P10; `draft` bis G1 hält das Lücken-Gate ehrlich — die Anforderungen zählen erst nach deiner Freigabe als verbindlich | projects/p10/**, intake.md v3, pm/T-0011 |
| B019 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Befund p9/T-0007 (SUP.9) sofort behoben:** Beim ersten verschachtelten Projekt fiel auf, dass `preflight.py` (board-check-Runde, Briefkasten-Prüfung) und `trace_matrix.py` (SWR-Quellen) weiterhin nur Top-Level-Ordner sahen — `projects/p10` wäre **still an zwei Konsistenz-Gates vorbeigelaufen**. Ursache: drei Kopien derselben Discovery-Logik; SWR-070 war nur in `aggregation.py` umgesetzt. Lösung: gemeinsame Auflösung `board.projekt_pfade()`, von Preflight und Matrix genutzt. 6 neue Tests, **194 Tests grün, Matrix 81/0** | als Befund melden und einplanen vs. sofort beheben | Ein blinder Fleck in einem Gate ist gefährlicher als ein rotes Gate — und der Fix ist deterministisch, klein und ohne Budgetwirkung (Klasse C, hier nur protokolliert) | board.py, preflight.py, trace_matrix.py, test_board/test_preflight/test_trace_matrix, p9/T-0007 |
| B020 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Zwei während der Session eingegangene Briefe sofort erledigt:** `pm/N-0017` (Cockpit zeigt wiederkehrende Aufgaben nicht) als **Umsetzungsfehler gegen SWR-074** eingestuft und behoben — Cockpit-Karte trägt jetzt dieselbe Klartext-Pille wie das Board statt eines „↻" ohne Legende (pm/T-0014, kein neuer SWR). `platform/N-0001` (board-check rot, „BOARD.md ist nicht aktuell") als **eigenständiges Problem** von pm/T-0010 abgegrenzt: Ursache ist die Push-Reihenfolge — Projekt-Workflows checken `platform@main` aus, alphabetisch wurde platform aber **nach** p0–p9 gepusht, also altes Werkzeug gegen neues Board-Format (SWR-074). `abschluss.cmd` pusht jetzt platform/process zuerst (pm/T-0013, bleibt `in_review` bis ein grüner Lauf vorliegt) | Symbol vs. Klartext; „schon behoben" abhaken vs. neu untersuchen | Der Mensch hatte recht, dass es *aussieht* wie T-0010 — es als Dublette abzuhaken hätte den echten, wiederkehrenden Wettlauf verdeckt; ein blinder Fleck in einem Gate wiegt schwerer als ein roter Lauf | app.js (Cockpit), abschluss.cmd, pm/T-0013, pm/T-0014, N-0017, platform/N-0001 |
| B021 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Agenda-Punkt 0 erledigt — pm/T-0012 (Brief N-0015) umgesetzt:** Der Kopfbereich zeigt statt der Auswahlliste über alle entdeckten Repos jetzt **anklickbare Einträge in denselben Gruppen wie das Cockpit** (Feste Teams · Projekt-Teams · Aktive Projekte), aktueller Eintrag hervorgehoben; abgeschlossene Projekte stehen hinter „weitere (n)" und klappen automatisch auf, wenn ein Deep-Link auf eines von ihnen zeigt. Kern der Lösung ist **eine gemeinsame serverseitige Einstufung** (`aggregation.einstufung`), die Cockpit und neuer Endpunkt `/api/navigation` teilen — Kopf und Cockpit können nicht mehr auseinanderlaufen (SWR-082, P9-Fläche v1.3). Deep-Links (`#/board/p3`) unverändert. **199 Tests grün, Matrix 82/0** | Gruppen im Frontend nachbauen vs. serverseitig ableiten; abgeschlossene Projekte ganz ausblenden vs. einklappen | Ein zweiter Frontend-Nachbau wäre genau der Fehler aus der Lesson vom 16.08. (jede Kopie derselben Logik muss nachgezogen werden) — deshalb eine Quelle. Ganz ausblenden hätte Boards, Berichte und Entscheidungslogs von p0–p9 im HMI unerreichbar gemacht | aggregation.py, server.py, app.js, index.html, test_org_cockpit.py, test_backend.py, p9-Requirements v1.3, pm/T-0012, N-0015 |
| D005 | 2026-08-16 | Mensch (E. John, via Session) | **Projekt-Pool eingeführt + LeLe je Sprint/Durchlauf statt quartalsweise:** Kandidatenliste versioniert (projekt-pool.md), Start-Knopf im HMI als CR T-0011; Lessons Learned ab sofort als Pflichtteil JEDES Sprint-/Takt-Abschlusses (Playbook Kap. 8 ergänzt), pm/T-0003 auf kontinuierlich umgestellt — schneller lernen, Fehler schneller beheben | Pool+LeLe-Takt | Wunsch des Auftraggebers | projekt-pool.md, T-0011, T-0003, Playbook |
| B022 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Brief N-0018 sofort umgesetzt:** Team-Chat zeigt die neuesten Nachrichten zuerst (SWR-083, P4-Fläche v1.1, pm/T-0015). Gedreht wird nur die **Anzeige** auf einer Kopie der Liste — die API liefert weiter chronologisch (SWR-050 unverändert). Das **Schreibfeld wandert mit nach oben**, weil es sonst hinter dem gesamten Verlauf läge; nicht verlangt, aber zwingende Folge — im Brief ausdrücklich benannt, damit der Auftraggeber widersprechen kann | nur drehen vs. drehen + Schreibfeld mitziehen; API-Reihenfolge ändern vs. nur Anzeige | Eine wörtliche Umsetzung ohne das Schreibfeld wäre in der Benutzung ein Rückschritt gewesen; die API zu drehen hätte alle Leser derselben Daten betroffen, ohne dass jemand danach gefragt hat | app.js (ladeChat), p4-Requirements v1.1, pm/T-0015, N-0018 |
| B023 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Brief `platform/N-0002` beantwortet und behoben:** Der ConnectionResetError-Traceback (WinError 10054) ist **kein Serverfehler** — ein LAN-Gerät (Handy) kappt die Keep-Alive-Verbindung beim Sperren/Tab-Schließen. Trotzdem behoben, weil ein Log mit harmlosen Tracebacks den nächsten echten unsichtbar macht: `Api.handle_one_request` und die neue Serverklasse `RuhigerServer.handle_error` fangen **nur** Verbindungsabbrüche (Reset/Abort/Broken Pipe) und schreiben eine Klartextzeile; echte Fehler behalten ihren Traceback. Unterscheidung in einer Prüffunktion `verbindungsabbruch()` statt zweier Fehlerlisten. Nebenbefund geprüft: Der Ruhe-Zähler der Neustart-Wache (SWR-073) wird auch im Abbruchfall freigegeben. Kein neuer SWR (Umsetzungsmangel an vorhandener Fläche), `pm/T-0016` | wegerklären vs. beheben; pauschales `except Exception` vs. nur Abbrüche | „Ist kein Fehler" hätte gestimmt und wäre trotzdem die falsche Antwort gewesen — der Schaden lag in der Lesbarkeit des Logs. Pauschales Fangen hätte künftige echte Befunde zugedeckt, deshalb zwei Tests, die genau das verhindern | server.py, test_backend.py (6 Tests), pm/T-0016, platform/N-0002 |
| B024 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **BEFUND beim Pflichtpunkt „Inbox prüfen": Der Klasse-A-DR `p10/T-0002` war unsichtbar** — `inbox.liste`, `inbox.historie`, `aggregation.uebersicht` und `dr_benachrichtigung._offene_drs` bauten den Projektpfad weiterhin als `root/<name>`; für das Sammel-Repo ergibt das `./p10` statt `./projects/p10` → **null Tickets, ohne Fehlermeldung**. Folge: Der G1-Antrag stand weder im Inbox-Reiter noch im Zähler (SWR-076), **und es wäre keine Frist-Warnmail (SWR-033/034) gelaufen** — am 23.08. hätte der Default G1a gegriffen, ohne dass der Mensch je gefragt worden wäre. Alle vier Stellen nutzen jetzt `aggregation.projekt_pfad`; 5 Regressionstests, die gegen den alten Code nachweislich scheitern. Kein neuer SWR (SWR-070 war richtig, nur unvollständig umgesetzt), `pm/T-0017`. **210 Tests, Matrix 83/0** | melden und einplanen vs. sofort beheben; Frist des DR verschieben vs. unverändert lassen | Eine Klasse-A-Entscheidung, die still per Default abläuft, verletzt Playbook Kap. 16 — das duldet keinen Aufschub. Die **Frist bleibt unverändert**: sie zu ändern wäre ein Eingriff in einen Klasse-A-Vorgang und steht dem Team nicht zu; stattdessen dem Auftraggeber offengelegt und eine neue Frist auf Wunsch angeboten | inbox.py, aggregation.py, dr_benachrichtigung.py, test_org_cockpit.py (5 Tests), pm/T-0017, p10/T-0002 |
| B025 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Lesson aus B019 verschärft (LeLe, pm/T-0003):** Die DoD-Prüffrage „Gibt es diese Auflösung noch ein zweites Mal im Repo?" galt bisher faktisch nur für die Skripte, in denen ein Befund auftrat — B024 zeigt, dass sie **für das gesamte Repo inkl. Backend** gelten muss (konkret: vor jeder Änderung an Discovery/Pfadauflösung `grep "join(root," / "join(wurzel,"` und jede Fundstelle nachziehen oder begründen). Zweite Lehre: **Ein leeres Werkzeugergebnis ist kein Beweis für „nichts zu tun"** — die Inbox meldete „0 offene Entscheidungen" und sah aus wie erledigte Arbeit; aufgefallen ist es nur, weil die Agenda eine konkrete Erwartung nannte. Regel: Wo ein Statusdokument eine Erwartung formuliert, wird sie gegen die Werkzeugausgabe geprüft, nicht abgeschrieben | Lesson als Einzelfall abhaken vs. Regel verschärfen | Derselbe Fehler ist innerhalb eines Tages zum zweiten Mal aufgetreten — die Lesson war richtig, nur zu eng angewendet | Playbook-Umlauf, pm/T-0003, pm/T-0017, p9/T-0007 |
| B026 | 2026-08-16 | PM-Team (Klasse B, via Session) | **Wunsch des Auftraggebers umgesetzt: Entscheidungen tragen Datum UND Uhrzeit** (SWR-084, P2-Fläche v1.1, `pm/T-0018`). Der Endpunkt schreibt `JJJJ-MM-TT HH:MM` (Ortszeit, Minutengenauigkeit) in Decision-Log-Zeile **und** Ticket-Vermerk — beide aus **einem** Aufruf von `entscheidungszeitpunkt()`, damit sie nicht über einen Minutenwechsel auseinanderlaufen; Uhr injizierbar für exakt prüfbare Tests. Die Historie-Ansicht des HMI zeigt die Uhrzeit dadurch ohne eigene Änderung mit. `frist`/`erstellt`/`geändert` bleiben reine Datumsfelder (Fristen gelten tagesgenau; `board.py` validiert dort ein Datumsmuster). **Uhrzeit der P10-Freigabe nachgetragen: 2026-08-16 08:18** — nicht geschätzt, sondern der Zeitstempel des Commits `cfab77c`, mit Herkunftsangabe im Log und im Ticket; Entscheidungsinhalt unangetastet. 4 Tests, **214 Tests, Matrix 84/0** | Uhrzeit überall vs. nur bei Entscheidungen; Alt-Eintrag ergänzen vs. date-only stehen lassen | Fristfelder mit Uhrzeit hätten die Board-Validierung gebrochen, ohne etwas zu gewinnen. Den Alt-Eintrag zu ergänzen ist kein Umschreiben einer Entscheidung (Kap. 16), sondern das Nachtragen einer fehlenden Angabe aus einer belegbaren Quelle — deshalb mit Herkunftsvermerk statt still | inbox.py, test_backend.py, p2-Requirements v1.1, pm/T-0018, projects/p10 D001 |
| B027 | 2026-08-16 | PM-Team (Klasse B, via Session) | **G1a verbucht (p10/T-0002 → done) — aber SWR-077–081 bleiben vorerst `draft`.** Der Intake-Vermerk hatte zugesagt: „Status stays `draft` until the G1 decision". Wörtlich befolgt hätte das jetzt fünf Anforderungen auf `reviewed` gesetzt, die weder gebaut noch getestet noch abgenommen sind — die SWR↔Test-Matrix hätte sie mangels Unit-Tests als „manuelle Abnahme dokumentiert" ausgewiesen und das **Lücken-Gate wäre grün geblieben, obwohl nichts dahintersteht** (nachgestellt und geprüft: 84 SWRs / 0 Lücken trotz leerer Umsetzung). Stattdessen wechselt jede SWR einzeln auf `reviewed`, sobald Sprint 1 sie mit ihrem Nachweis liefert; die Zusage im Traceability-Vermerk wurde entsprechend korrigiert und begründet | Zusage wörtlich einlösen vs. Gate ehrlich halten | Eine Freigabe beauftragt den Sprint — sie bezeugt nicht, dass etwas verifiziert ist. Genau dieser Kurzschluss (Gate grün ohne Substanz) war heute schon zweimal Befund (p9/T-0007, pm/T-0017); ihn sehenden Auges ein drittes Mal zu bauen, wäre der schlechtere Dienst | projects/p10/requirements (Traceability), projects/p10/tickets/T-0002.md, pm/T-0018 |
| B028 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Brief pm/N-0019 sofort umgesetzt: Requirements aller Projekte/Teams sichtbar und filterbar** (SWR-085, P1-Fläche v1.2, `pm/T-0019`). Die Ansicht war seit P1 auf **ein** Projekt gescopt (SWR-030) — bei 13 Projekten/Teams mit 22 Dokumenten sah sie vollständig aus und war es nie. `/api/requirements?projekt=alle` liefert jetzt alles in einer Antwort, jede Datei mit Projekt **und** Cockpit-Gruppe; Filter nach Projekt/Team, Gruppe und Volltext, Zähler „n von m aus k". Gruppenzuordnung aus `aggregation.einstufung` — derselben Quelle wie Cockpit und Kopfbereich (SWR-082), keine zweite Einstufungslogik. Gescopte Einzelabfrage per Regressionstest unverändert. 3 Tests | Sammelwert `alle` am vorhandenen Endpunkt vs. zweiter Endpunkt; Default „alles" vs. Default „aktuelles Projekt" | Ein zweiter Endpunkt hätte dieselbe Ladelogik ein zweites Mal geführt (Lesson B025). Default „alles" ist die wörtliche Bitte des Auftraggebers und die ehrlichere Voreinstellung — ein Ausschnitt, der sich als Ganzes ausgibt, war genau der Fehler | aggregation.py, server.py, app.js, test_backend.py, p1-Requirements v1.2, pm/T-0019, pm/N-0019 |
| B029 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Brief pm/N-0020 — BEFUND in eigener Sache: Die zugesagte Pool-Ansicht war nie beauftragt.** `pm/D005` nannte „Start-Knopf im HMI als CR T-0011"; `pm/T-0011` trägt seit demselben Tag den P10-Entscheidungsantrag. Die Nummer war im Beschluss vergeben, bevor das Ticket existierte, und wurde von einem anderen Thema belegt — kein Vorgang trug die Zusage, und es fiel niemandem auf, weil nichts Offenes existierte. **Teil 1 geliefert:** eigener Reiter „Projekt-Pool" neben dem Cockpit, `/api/pool` liest `pm/management/projekt-pool.md` read-only nach Kategorien, geparst mit dem **vorhandenen** Tabellenparser (SWR-086, P9-Fläche v1.4, `pm/T-0020`), Kopfzeile benennt offen „Anzeigen: da · Anlegen/Starten: noch nicht". **Teil 2 (Anlegen/Starten) bewusst zurückgestellt** an `pm/T-0022`, bis der geprüfte Schreibpfad aus P10 Sprint 1 steht. Neue LeLe-Regel: eine Ticketnummer in einem Beschluss ist keine Beauftragung — Beschlüsse, die Arbeit ankündigen, werden in derselben Session mit einem real angelegten Ticket belegt. 3 Tests | alles drei jetzt bauen vs. Anzeigen jetzt / Schreiben nach P10; Versäumnis erklären vs. offen benennen | Ein zweiter Schreibpfad neben dem gerade entstehenden aus P10 wäre exakt die Doppelung, die am selben Tag zweimal Befund war (p9/T-0007, pm/T-0017). Das Versäumnis offen zu benennen ist Pflicht — der Auftraggeber hat zweimal nach etwas gefragt, das wir zugesagt und nie eingeplant hatten | aggregation.py, server.py, app.js, test_backend.py, p9-Requirements v1.4, pm/T-0020, pm/T-0022, pm/N-0020, pm/T-0003 (LeLe) |
| B030 | 2026-08-16 | PM-Team (Klasse B, via Routine-Session) | **Brief platform/N-0003 umgesetzt: eindeutige Aufgaben-Kennung `<projekt>/T-xxxx`** (SWR-087, P9-Fläche v1.4, `pm/T-0021`). Ticketnummern sind nur je Repo eindeutig (`T-0002` in pm, p2, p3, p4, p9, p10); Inbox und Historie zeigten das Repo mit, Board-Karte, Ticket-Detail und Cockpit nicht. `aggregation.ref()` bildet die Kennung an **einer** Stelle, alle sechs Ansichten benutzen sie; Schreibkonvention in Briefen/Agenda/Status entsprechend. **Ticketdateien werden NICHT umbenannt:** eine Umnummerierung über alle Repos macht jede Decision-Log-Zeile, jeden Brief, jeden Report und die Git-Historie falsch oder erzwingt das Umschreiben von Vergangenem (Kap. 16 schließt das für Entscheidungen aus); zusätzlich hat eine Formatänderung am generierten BOARD.md heute Morgen bereits alle board-check-Workflows rot gemacht (pm/T-0013). Dem Auftraggeber offengelegt, mit dem Angebot, eine echte Migration als Entscheidung mit Optionen vorzulegen. 3 Tests, darunter der Briefkasten-Fall (dieselbe Nummer in zwei Repos → zwei Kennungen) | Anzeige qualifizieren vs. Dateien umnummerieren vs. Repo-Präfix im Dateinamen; selbst entscheiden vs. als DR vorlegen | Die Anzeige löst die berichtete Verwirrung vollständig und bricht keinen Verweis — eine Migration löst dieselbe Verwirrung und bricht alle. Die Anzeigeänderung ist reversibel und ohne Außenwirkung (Klasse B/C); die Migration wäre es nicht, deshalb wird sie nicht nebenbei gemacht, sondern nur auf ausdrücklichen Wunsch als Entscheidung vorgelegt | aggregation.py, inbox.py, app.js, test_backend.py, p9-Requirements v1.4, pm/T-0021, platform/N-0003 |
| B031 | 2026-08-16 09:38 | PM-Team (Klasse B, via Routine-Session) | **BEFUND: Eine ganze Session war fertig und blieb unverbucht — ein leerer `index.lock` in allen 15 Repos hat sie blockiert.** Die Session um 08:37 lieferte B028/B029/B030 (SWR-085/086/087) vollständig ab, schrieb Statusbericht und Agenda — und committete nichts davon. Ursache: Git legt vor jeder Index-Operation `.git/index.lock` an; auf dem Cowork-Mount schlägt das anschließende **Löschen** fehl (`unable to unlink … Operation not permitted`), der Lock bleibt liegen, **jeder weitere Commit im Repo bricht ab**. `preflight.py` hat die Lage korrekt erkannt und gemeldet, konnte aber nur auf eine Handlung am Host verweisen — die Session konnte lesen, dass sie blockiert ist, und nichts dagegen tun. **Zwei Schritte:** (1) Arbeit der Vorsession gegen die Werkzeuge verifiziert (223 Tests, Matrix 87/0, Katalog + Architekturbild konsistent) — **nicht** gegen ihre eigene Beschreibung (Lesson B025) — und in fünf Commits entlang der Repo-Grenzen nachverbucht. (2) `preflight.entferne_artefakte()` arbeitet jetzt zweistufig: `os.remove`, und wo der Mount das verbietet, **Wegräumen nach `.git/verwaiste-locks/`** — auf diesem Mount funktioniert `rename`, und für Git ist der Lock damit ebenso weg. Geparkt zählt bewusst **nicht** als Befund (Git ist entsperrt), der Hinweis auf die fortbestehende Ursache bleibt. 7 Regressionstests, die `os.remove` abschalten und gegen den alten Code nachweislich scheitern. **Zweiter Befund im selben Vorgang — der erste Fix war unvollständig:** Der Commit direkt nach einem grünen Preflight scheiterte erneut. Git legt bei *jedem* Aufruf einen `index.lock` an, auch bei `git status` — Preflight räumte am Anfang alle Locks weg, erzeugte durch seine eigenen Prüfungen neue und meldete darüber STARTKLAR. Die Räumung läuft jetzt **zweimal** je Lauf (Anfang + Schlusslauf nach allen Prüfungen), dazu `--nur-locks` zum schnellen Entsperren vor einem einzelnen git-Schreibvorgang. Gefunden nur, weil der Nachweis am echten System geführt wurde statt nur im Test. Preflight ist Prozess-Tooling (CR T-0024) — kein SWR, keine Matrix-Wirkung. `pm/T-0023`. **230 Tests, Matrix 87/0, 0,00 € API** | Nur melden und auf den Menschen warten vs. selbst arbeitsfähig werden; Lock löschen erzwingen vs. wegbenennen; Vorsession-Arbeit verwerfen vs. prüfen und nachverbuchen; Räumung nur am Anfang vs. zusätzlich als Schlusslauf | Ein Werkzeug, das eine Blockade nur beschreibt, statt den offenen Weg daneben zu nehmen, ist die Hälfte der Arbeit — `rename` war die ganze Zeit erlaubt. Die Vorsession-Arbeit zu verwerfen hätte 45 Minuten geprüfte Lieferung vernichtet; sie ungeprüft zu übernehmen wäre der Fehler aus B025 gewesen, deshalb erst Tests/Matrix/Gates, dann Commit | preflight.py, test_preflight.py (5 Tests), pm/T-0023, Runbook Kap. 5 (R7), Nachverbuchung platform/pm/p0/p1/p9 |
| B032 | 2026-08-16 09:38 | PM-Team (Klasse B, via Routine-Session) | **P10 Sprint 1 in dieser Session NICHT begonnen — bewusst, mit Begründung.** Agendapunkt 1 war der Hauptpunkt; die Session ist stattdessen vollständig in die Wiederherstellung der verlorenen Verbuchung und deren Ursache gegangen (B031). Sprint 1 baut den **ersten zweiten Schreibpfad auf Tickets** (ADR-007, Schreibendpunkt mit Fingerprint und Sofort-Commit) — ausgerechnet die Sorte Arbeit, die man nicht halb gebaut liegen lässt, und ausgerechnet in einer Session, in der die Commit-Fähigkeit gerade erst wiederhergestellt wurde. Punkt 1 bleibt unverändert oben auf der Agenda, mit voller Zeit in der nächsten Session | Sprint 1 anteilig anfangen (nur ADR-007) vs. geschlossen verschieben | Ein Architekturentscheid für einen Schreibpfad, nebenbei am Ende einer Aufräum-Session geschrieben, hätte die Umsetzung schlechter verankert als eine Stunde später mit freiem Kopf; ein halb gebauter Schreibpfad ist zudem genau das Risiko, gegen das die Konflikterkennung (SWR-080) erst noch entstehen muss | pm/management/session-agenda.md, projects/p10 |
| B033 | 2026-08-16 10:05 | PM-Team (Klasse B, via Routine-Session) | **P10 Sprint 1 geliefert: Aufgaben lassen sich im HMI bearbeiten** (ADR-007, SWR-077–081, `p10/T-0003`). Der zweite Schreibpfad auf Tickets liegt **in der Skript-Route selbst** (`board.aktualisiere` in `platform/scripts/board.py`) — `backend/tickets.py` ist reine Fassade (Projektauflösung, HTTP-Codes, Commit, Rücknahme). Konflikte gegen die parallele 30-Minuten-Session über einen Inhalts-**Fingerabdruck** statt über Dateisperren; ein gescheiterter Commit schreibt Ticketdatei **und** BOARD.md byteweise zurück. Endpunkte `GET /api/ticket/editor` (PIN-frei) und `POST /api/ticket` (durch den vorhandenen Schreibschutz aus ADR-006); HMI mit Editor, Label-Pillen und Label-Filter. Nebenbefund gleich mitbehoben: die Zeitstempel-Formatierung aus SWR-084 lag in `inbox.py` und wurde für die Historie ein zweites Mal gebraucht — `inbox.entscheidungszeitpunkt` delegiert jetzt an `board.zeitpunkt`, eine Quelle. **SWR-077–081 einzeln auf `reviewed`**, jede mit ihrem Nachweis (B027 eingehalten). 33 Tests, **263 Tests, Matrix 87/0, 0,00 € API**, ~115 min Ist gegen 120 min | Validierung im Server nachbauen vs. `board.py` erweitern; Dateisperre vs. Fingerabdruck; Labels als BOARD.md-Spalte vs. nur im HMI; `frist` editierbar vs. draußen lassen | Eine Prüfschicht im Server wäre die zweite Kopie derselben Logik gewesen — Risiko R2 des Sprint-0-Plans und die Lesson vom Vormittag. Eine Sperrdatei auf einem Mount, der Dateien nicht löschen lässt (R7, heute Vormittag real), wäre schlimmer als der Konflikt, den sie verhindern soll. Eine neue Board-Spalte ist eine Formatänderung — genau die hat heute früh alle board-check-Workflows rot gemacht (pm/T-0013), das gehört gebündelt | board.py, backend/tickets.py, server.py, aggregation.py, inbox.py, app.js, test_p10_editor.py (33 Tests), adr-007, komponenten.yaml, architektur.md v1.5, projects/p10 (T-0003, T-0004, Sprint-1-Plan+Report, Requirements v1.1) |
| B034 | 2026-08-16 10:05 | PM-Team (Klasse B, via Routine-Session) | **G4 wird nicht vom Team abgenommen, sondern vorgelegt:** `p10/T-0004` liegt als Decision Request in der Inbox (Optionen G4a/G4b/G4c, Frist 2026-08-23, Default G4a), die **Baseline `p10-v1.0` bleibt ungesetzt**, bis entschieden ist. Im Antrag stehen zwei Punkte ausdrücklich zum Widerspruch: Labels erscheinen nicht im generierten `BOARD.md`, und `frist` wurde ohne Auftrag mit-editierbar gemacht (sonst zwänge jede Terminverschiebung zurück in die Skript-Route) | selbst abnehmen (Sprint-Ergebnis ist grün) vs. vorlegen; Abweichungen still lassen vs. benennen | Projektabnahmen sind Klasse A (Playbook Kap. 16) — dass das Ergebnis grün ist, macht die Entscheidung nicht zu einer des Teams. Eine Abweichung vom Auftrag, die niemand benennt, ist kein Feature, sondern ein späterer Befund | projects/p10/tickets/T-0004.md, projects/p10/management/sprint-1/ |
| B035 | 2026-08-16 10:12 | PM-Team (Klasse B, via Routine-Session) | **G4a verbucht — P10 ist abgenommen (D002, 2026-08-16 10:02, via Inbox).** Die Entscheidung fiel **14 Sekunden nach** dem Sprint-Commit dieser Session; der Auftraggeber hat den Antrag im HMI gesehen und sofort entschieden — der erste vollständige Roundtrip Lieferung → Antrag → Knopf → Abnahme innerhalb einer Session. Verbucht: `p10/T-0003` und `p10/T-0004` auf `done`, Abschlussbericht, Steckbrief auf `abgeschlossen`, **Baseline `p10-v1.0` auf `projects` und `platform`**. Der DR wurde über die im Playbook erlaubten Übergänge geschlossen (`open → in_progress → in_review → done`) statt per direkter Statussetzung — die Abkürzung wäre möglich gewesen und hätte die Übergangsregel genau dort umgangen, wo sie gilt. Zehntes abgeschlossenes Projekt, erstes vollständig im Sammel-Repo `projects` (pm/D003) | DR direkt auf `done` setzen (Präzedenz p10/T-0002) vs. über die erlaubten Übergänge; Baseline sofort vs. erst nach den Stichproben | Die Abnahme lag vor, damit ist die Baseline fällig — die sieben Stichproben sind Betriebsnachweis des Menschen und blockieren die Baseline nicht (Muster P9). Die direkte Statussetzung hätte funktioniert, weil die Übergangsprüfung gegen `HEAD` läuft und eine mitcommittete Änderung damit unsichtbar wird; das ist kein Grund, sie zu nutzen | projects/p10 (T-0003, T-0004, Abschlussbericht, steckbrief.yaml), Tag p10-v1.0 auf projects + platform |
| B036 | 2026-08-16 10:12 | PM-Team (Klasse B, via Routine-Session) | **Brief `platform/N-0004` beantwortet — und ein Ablauffehler dabei entdeckt.** Der Auftraggeber verlangt den „Anlegen/Starten"-Knopf im Projekt-Pool (`pm/T-0022`), der seit der P10-Abnahme entsperrt ist. Beantwortet mit einer **Rückfrage statt einer Annahme**: „Starten" ist eine Projektbeauftragung und damit Klasse A — Variante **A** (Knopf legt Ordner an und stellt einen G0-DR in die Inbox, Empfehlung) oder Variante **B** (der Knopfdruck ist die Entscheidung, mit Entscheider-Identität und Log-Zeile, wie die Inbox-Buttons seit P3). Ohne Antwort wird A gebaut. Team-Gründungen bleiben draußen (Steckbrief mit Profil/Rollen/**Datenklasse**/Zugängen — bei `team-steuer`/`team-trading` hängt daran mehr als ein Ordner). **Ablauffehler:** Der Brief kam um 10:04 herein, also **während** der Session; der Pflichtpunkt „Briefkasten zuerst" hatte um 09:5x sauber „keine offenen Briefe" gemeldet. Aufgefallen ist er nur zufällig, weil beim Setzen der Baseline ein fremder Commit im `platform`-Log stand. Dasselbe galt für die Inbox: die G4a-Entscheidung fiel 14 Sekunden nach dem Sprint-Commit. **Neue Regel in der Agenda (Punkt 0): Briefkasten und Inbox werden vor dem Abschluss ein zweites Mal geprüft** — eine Session dauert länger als der Blick an ihrem Anfang gültig ist | Variante selbst wählen vs. fragen; Brief in dieser Session beantworten vs. der nächsten überlassen; Zweitprüfung als Einzelfall abhaken vs. als Regel verankern | Ein Knopf, der ein Projekt beauftragt, ohne dass geklärt ist, ob der Klick die Entscheidung *ist*, wäre eine Klasse-A-Mechanik nach Bauchgefühl. Der Brief lag bereits im Repo — ihn 30 Minuten liegen zu lassen, obwohl die Session noch lief, wäre die Sorte Versäumnis gewesen, die nur deshalb keine Folgen hat, weil der Takt kurz ist | platform/management/briefkasten/N-0004.md, pm/tickets/T-0022.md, pm/management/session-agenda.md |
| B037 | 2026-08-16 10:18 | PM-Team (Klasse B, via Routine-Session) | **Brief `platform/N-0005` beantwortet — Kandidaten anlegen kommt ohne Rückfrage.** Der Auftraggeber will neue Pool-Kandidaten **selbst** eintragen, in **beide** Kategorien (Team und Technik). Anders als das „Starten" aus `platform/N-0004` ist das Arbeitsebene ohne Außenwirkung: einen Namen in eine Liste zu schreiben beauftragt nichts. Deshalb kein DR, sondern direkt in `pm/T-0022` gezogen — mit zwei Festlegungen, die im Brief offengelegt sind: die Kandidatennummern laufen über beide Kategorien durch, und das Formular fragt die Felder der **jeweiligen** Tabelle ab (Team: Nutzen + Voraussetzung; Technik: Quelle), statt beide Tabellen zu vereinheitlichen. **Löschen wird nicht gebaut** (bleibt Zuruf) — eine Liste mit Ein-Klick-Löschen verliert Einträge, an die sich später niemand erinnert. **Zweiter Fund der neuen Regel aus B036:** Auch dieser Brief kam während der Session herein (10:05) und wäre ohne die Zweitprüfung vor dem Abschluss liegen geblieben — die Regel hat sich in derselben Sitzung bezahlt gemacht, in der sie entstanden ist | als DR vorlegen vs. direkt einplanen; beide Tabellen vereinheitlichen vs. Felder je Kategorie; Löschen mitbauen vs. weglassen | Für etwas ohne Außenwirkung einen Entscheidungsantrag zu stellen, wäre Bürokratie, die den Auftraggeber Zeit kostet und nichts schützt. Die Tabellen zu vereinheitlichen hätte den bestehenden Pool umgeschrieben, um ein Formular zu vereinfachen — falsche Richtung | platform/management/briefkasten/N-0005.md, pm/tickets/T-0022.md, pm/management/session-agenda.md |
| B038 | 2026-08-16 10:23 | PM-Team (Klasse B, via Routine-Session) | **Brief `pm/N-0021` beantwortet — BEFUND: Der Auto-Push stand seit 09:44 still, und gemerkt hat es der Auftraggeber, nicht die Automatik.** Die Frage lautete, warum ein paar pm-Tickets „seit längerem in review" stehen. Antwort: `pm/T-0010` und `pm/T-0013` warten beide auf einen **grünen GitHub-Actions-Lauf** — der seit 09:44 nicht mehr stattfinden konnte. Der Wächter brach dreimal in Folge ab (09:44, 09:59, 10:14), letzter erfolgreicher Push **08:30**; liegengeblieben: **21 Commits** und die Baseline-Tags `p10-v1.0` auf `projects` und `platform`, also die Abnahme des zehnten Projekts. **Ursache (`pm/T-0024`), zwei Fehler, die einander deckten:** (1) `git_prozess_aktiv()` liest `tasklist` mit dem cp1252-Default, `tasklist` antwortet in der OEM-Codepage 850. Findet der Filter **kein** git.exe — der Regelfall — lautet die Antwort „… Kriterien **ausgeführt**."; das `ü` ist Byte 0x81, der Reader-Thread stirbt, der Fehler fällt in `except Exception: return True` und die Funktion meldet „Git läuft", wenn gerade keiner läuft. (2) `test_nur_locks_laesst_repos_unberuehrt` prüft ein Fake-Repo in %TEMP%, fragte dafür aber den **gesamten Rechner** nach Git-Prozessen ab — ein Commit aus dem HMI oder der Wächter selbst machten ihn rot. Beide Wege enden im selben `AssertionError: 1 != 0`, welcher am 10:14 zuschlug, ist nicht mehr trennbar; beide behoben: `errors="replace"`, ein `except`, das sich **meldet** statt zu raten, und `git_prozess_aktiv` in `setUp` festgenagelt. 4 neue Tests mit `subprocess`-Doppel und echten CP850-Bytes; **Gegenprobe gegen den alten Code: 2 von 4 scheitern nachweislich**, zusätzlich der Abbruch von 10:14 exakt nachgestellt. **267 Tests, Matrix 87/0, 0,00 € API.** Preflight ist Prozess-Tooling (`p0/T-0024`) — kein SWR | Tickets auf `done` setzen (die Arbeit ist ja getan) vs. `in_review` mit Vermerk lassen; nur den Test hermetisch machen vs. auch die Codepage; Warnmail für den Wächter gleich mitbauen vs. als CR vormerken | Die beiden Tickets zu schließen wäre die bequeme Antwort auf den Brief gewesen und genau der Fehler aus B025: Ein Fremdnachweis wird nicht dadurch erbracht, dass jemand nachfragt. Nur den Test zu reparieren hätte den Codepage-Fehler als stille Falschmeldung stehen lassen — er hätte beim nächsten echten Lock-Fall zugeschlagen. Eine Warnmail hat Außenwirkung auf das Postfach und gehört nicht nebenbei in einen SUP.9-Fix | preflight.py, test_preflight.py (4 Tests), pm/T-0024, pm/T-0010 + pm/T-0013 (Vermerke), pm/N-0021, Betriebs-Backlog (CR Wächter-Warnmail) |
| B039 | 2026-08-16 10:23 | PM-Team (Klasse B, via Routine-Session) | **`pm/T-0022` (Projekt-Pool: Anlegen/Starten) in dieser Session bewusst NICHT begonnen — Reihenfolge, nicht Zeitmangel.** Der Punkt stand als Hauptpunkt oben auf der Agenda und bleibt dort. Grund für den Vorzug des Push-Befunds (B038): Solange der Wächter abbricht, erzeugt **jede** Feature-Session Arbeit, die nach außen nicht existiert — genau das Muster, das heute früh schon einmal eine komplette Session gekostet hat (B031). Ein Formular, das Pool-Kandidaten schreibt, hätte in dieser Lage sauber gebaut, getestet und committet werden können und wäre trotzdem nicht auf GitHub angekommen; die board-check-Workflows hätten es nie gesehen. Erst der Weg nach außen, dann neue Fläche. **Zum „Starten"-Knopf:** Auf die Rückfrage aus `platform/N-0004` (Variante A oder B) liegt **keine Antwort** vor — der einzige neue Brief (`pm/N-0021`) betrifft ein anderes Thema. Es bleibt bei der angekündigten Regel: ohne Antwort wird **A** gebaut (Knopf legt den Ordner an und stellt einen G0-DR in die Inbox). Der Teil „Anlegen" ist davon unabhängig und unblockiert | Anlegen-Teil anfangen und Rest verschieben vs. geschlossen verschieben; auf die A/B-Antwort warten vs. Default A ansetzen | Ein angefangenes Formular über eine Sessiongrenze zu tragen, während parallel eine zweite Routine-Session laufen kann, ist die Konstellation, für die P10 überhaupt erst eine Konflikterkennung gebraucht hat. Auf die A/B-Antwort zu warten wäre falsch — die Rückfrage hatte den Default ausdrücklich benannt, und ein Default, der still auf Antwort wartet, ist keiner | pm/tickets/T-0022.md, pm/management/session-agenda.md, platform/N-0004 |
| B040 | 2026-08-16 10:32 | PM-Team (Klasse B, via Routine-Session) | **Brief `team-mail/N-0002` beantwortet — echter Befund, und zwei Sessions sind dabei aufeinandergestoßen.** Der Auftraggeber wollte beim „Jetzt zusammenfassen" dieselben Einstellungen wie gespeichert. Von seinen zwei Punkten war nur einer ein Fehler, dieser aber handfest: **Der KI-Hinweis (SWR-072) und die Modellwahl (SWR-071) wirkten die ganze Zeit** — `--jetzt` lädt `konfiguration.yaml` bei jedem Lauf frisch. **Der Takt wirkte nicht:** `--jetzt` rief fest `lauf_takt(1, cfg)`, während das Team auf `takte: [7]` steht — jeder Klick erzeugte einen **Tages**-Digest statt des konfigurierten **Wochen**-Digests, erkennbar ausschließlich am Dateinamen, ohne Fehlermeldung. Behoben in `team-mail/T-0003` (`jetzt_takte(cfg, tage=None)`: ohne Override laufen alle gespeicherten Takte, SWR-064; `--tage` bleibt Einzel-Override), Regressionstest über `[7]→[7]`, `[1,7,30]→[1,7,30]`, leer→`[1]`, gegen den alten Code scheitert der erste Fall. **Kein neuer SWR** — SWR-063 war nie auf einen Tag festgelegt, das war eine stille Annahme der Erstimplementierung. **268 Tests grün.** Rest als `pm/T-0025`: Klartextzeile am Knopf, womit er läuft (Modell, Hinweis, Takte) — beide Teile des Briefes hatten dieselbe Wurzel, nämlich dass nichts über den Zustand spricht. **Kollision zweier Routine-Sessions:** An diesem Brief arbeiteten zwei Sessions gleichzeitig — die Überlappung, vor der die Agenda seit heute früh warnt. Die eine fand und behob den Takt-Fehler, die andere hatte den Satz als Wunsch nach einem **Auswahlmenü** gelesen und wollte eine Rückfrage stellen. Diese Lesart war die schwächere: „die gleichen einstellungen wie gespeichert" heißt *nimm die Konfiguration*, nicht *frag mich nochmal*. Zusammengeführt, Rückfrage entfällt, Ticket auf den verbleibenden Teil zugeschnitten | Auswahl am Knopf vs. alle gespeicherten Takte; fremde uncommittete Arbeit übernehmen vs. neu bauen vs. verwerfen; Rückfrage stellen vs. Wortlaut folgen | Ein Auswahlmenü hätte den Auftraggeber bei jedem Klick erneut nach etwas gefragt, das er längst eingestellt hat. Die fremde Arbeit wurde **gegen die Werkzeuge geprüft, nicht gegen ihre Beschreibung** (B025) — Ticket, Implementierung und Regressionstest lagen vollständig vor, 268 Tests grün — und deshalb übernommen statt ein zweites Mal gebaut; sie zu verwerfen hätte den einzigen Teil zerstört, der den tatsächlichen Fehler traf | team-mail/tools/mail_digest.py, team-mail/tickets/T-0003.md, platform/tests/test_mail_autopilot.py, team-mail/N-0002, pm/tickets/T-0025.md |
| B041 | 2026-08-16 10:40 | PM-Team (Klasse B, via Routine-Session) | **Zwei Routine-Sessions sind an demselben Brief kollidiert — und der richtige Befund wäre dabei fast verlorengegangen.** Ablauf: Diese Session beantwortete `team-mail/N-0002` und legte `pm/T-0025` an; parallel hatte eine zweite Session denselben Brief bearbeitet, den **tatsächlichen** Fehler gefunden (`--jetzt` lief fest auf 1 Tag, Team steht auf `takte: [7]`) und ihn samt Ticket `team-mail/T-0003`, Implementierung und Regressionstest gebaut. Bemerkt wurde die Überlappung erst beim Abschluss-Check über eine fremde Änderung an `mail_digest.py`, die diese Session nie angefasst hatte. Beim Prüfen zog die Parallelsession ihre Arbeit **selbst zurück** (nach `.verworfen/`, Vermerk „Konflikt mit pm/T-0025") — der Board-Check meldete plötzlich 2 statt 3 Tickets, `jetzt_takte` war aus Code und Test verschwunden. Ergebnis wäre gewesen: eine Brief-Antwort, die auf ein Ticket verweist, das es nicht gibt (exakt die Lesson B029), und ein Befund, der niemandem mehr auffällt. **Entscheidung: Befund neu geführt und selbst gebaut** — Analyse gegen die Werkzeuge, eigener Regressionstest, Gegenprobe gegen den alten Code (`Lists differ: [1] != [7]`), `team-mail/T-0003` mit ausdrücklichem Herkunftsvermerk auf die Parallelsession. **268 Tests, Matrix 87/0.** Die Lesart des Briefes stammt von dort und war die bessere; das gehört benannt, nicht eingemeindet | fremde Arbeit unverändert übernehmen vs. neu führen vs. auf die Parallelsession vertrauen; Herkunft nennen vs. still liefern | Auf die Parallelsession zu vertrauen hätte den Befund verschwinden lassen — sie hatte sich bereits zurückgezogen. Sie unverändert zu übernehmen wäre die Verbuchung ungeprüfter Arbeit gewesen (B025). Die Herkunft zu verschweigen wäre bequem und der Anfang davon, dass niemand mehr weiß, worauf eine Lieferung beruht | team-mail/tools/mail_digest.py, team-mail/tickets/T-0003.md, platform/tests/test_mail_autopilot.py, team-mail/N-0002, pm/tickets/T-0025.md, pm/management/session-agenda.md |
| B042 | 2026-08-16 10:45 | PM-Team (Klasse B, via Routine-Session) | **Brief `platform/N-0006` (roter CI-Lauf) beantwortet — BEFUND: Das Matrix-Gate sieht das Sammel-Repo `projects` nicht.** Der Auftraggeber meldete Job `tests`, `exit code 1` zum Commit `cc5d277`. Nachgestellt in zwei Umgebungen, die den CI-Checkout nachbauen: mit `projects` **87 SWRs / 0 Lücken, exit 0**; mit exakt der Repo-Liste aus `ci.yml` **82 SWRs / 5 Lücken, exit 1**. Die Unit-Tests sind in beiden Fällen grün (267 im gepushten Stand, 268 aktuell), auch ohne `team-mail` wie auf dem Runner — gescheitert ist der **Matrix-Schritt**, nicht der Codepage-Fix, auf dessen Commit der Lauf steht. **Ursache:** `ci.yml` checkt zwölf Repos aus, `projects` kommt **null mal** vor; seit `pm/D003` liegen Projekte als Ordner darin, P10 hat dort SWR-077–081. `trace_matrix.py` findet sie (SWR-070, `p9/T-0007`) — auf dem Runner existiert das Verzeichnis nicht, also fehlen genau die fünf P10-Anforderungen. **Rot ist der Workflow seit der P10-Freigabe um 08:18; sichtbar wurde es erst, als der Push um 10:30 wieder lief** — zwei Befunde desselben Vormittags, von denen der eine (`pm/T-0024`, stehender Push) den anderen verdeckt hielt. Behoben: `projects` als Checkout ergänzt, Begründung im Workflow-Kopf. **Dieselbe Lesson wie `p9/T-0007`, nur eine Dateiart weiter: Die letzte Kopie der Regel war kein Python-Code, sondern YAML** — die damalige Suche (`grep "join(root,"`) konnte Workflows nicht finden. `pm/T-0026`, bleibt `in_review` bis ein grüner Actions-Lauf vorliegt | Fix vorab als erledigt verbuchen vs. `in_review`; nur `ci.yml` reparieren vs. alle Workflows mit Repo-Listen jetzt durchgehen; PAT-Erweiterung selbst annehmen vs. als Handlung benennen | Ein Gate, das nur lokal grün ist, war exakt dieser Befund — ihn mit einem lokal grünen Lauf abzuhaken hätte den Fehler wiederholt, während man ihn beschreibt. Die anderen Workflows im selben Zug umzubauen wäre eine Formatänderung an allen Prüfstrecken auf einmal, und genau das hat heute früh schon einmal sämtliche board-checks rot gemacht (`pm/T-0013`) — deshalb als CR-Kandidat statt als Nebenarbeit. Der PAT-Scope ist Zugangsverwaltung und liegt beim Menschen (Kap. 16, Klasse A) | platform/.github/workflows/ci.yml, pm/tickets/T-0026.md, platform/N-0006, Betriebs-Backlog (CR Repo-Listen in Workflows) |
| B043 | 2026-08-16 14:05 | PM-Team (Klasse B, via Routine-Session) | **Brief `pm/N-0025` beantwortet — BEFUND in eigener Sache: Ein offenes Ticket blieb über fünf Sessions liegen, und der Vorwurf des Auftraggebers trifft zu.** `pm/T-0025` (Rest aus `team-mail/N-0002`) stand seit der 10:40-Session offen, ohne dass eine der folgenden fünf Routine-Sessions es aufgriff — die Agenda nannte es seit der 10:23-Session (Punkt 0c), aber nur als Randnotiz, nie mit Priorität. **Zwei strukturelle Lücken identifiziert:** Backlog-Tickets (CR/Problem) haben kein Fristfeld (nur Decision-Requests haben `frist`+`default`) — Priorität steuert nur die Reihenfolge *innerhalb* einer Session, nicht *ob* eine Session das Ticket überhaupt aufgreift; und „wiederkehrend" bedeutet bisher nur „je Sessionlauf geprüft", nicht „zu einer bestimmten Uhrzeit" — ein Wunsch wie „jeden Tag um 14 Uhr" ist mit `takt: je-session` nicht ausdrückbar. **Nicht sofort gebaut:** Ein Fristfeld für Backlog-Tickets und ein echter Uhrzeit-Takt sind Entwurfsentscheidungen mit Wirkung auf jedes künftige Ticket und auf die Session-Taktung selbst (F14) — als `pm/T-0030` für eine dafür vorgesehene Design-Session eingeplant, mit Entwurf im Ticket. **Sofort umgesetzt:** `pm/T-0025` Priorität mittel → hoch, jetzt oben auf der Agenda statt als Randnotiz | Fristfeld/Uhrzeit-Takt sofort improvisieren vs. als Design-Session einplanen; T-0025 nur benennen vs. auch die Priorität korrigieren | Ein Fristfeld ohne geklärte Konsequenz bei Überschreitung (Eskalation? Neu-Priorisierung? nichts?) wäre die Sorte schnell gebautes Werkzeug, das Termine anzeigt, sie aber nicht durchsetzt — B025/B038. Den Befund nur zu benennen, ohne die konkrete Ursache (T-0025s Priorität) zu korrigieren, hätte denselben Fehler in der nächsten Session wiederholt | pm/tickets/T-0030.md, pm/tickets/T-0025.md (Priorität), pm/tickets/T-0003, pm/management/session-agenda.md, pm/N-0025 |
| B044 | 2026-08-16 15:20 | PM-Team (Klasse B, via Routine-Session) | **`pm/T-0030` Teil 1 geliefert — Backlog-Tickets sind ab jetzt terminierbar, und eine gerissene Frist ist sichtbar (SWR-091, P9-Fläche v1.7).** Die Session war frei (Briefkasten leer, keine entschiedene Inbox-Entscheidung), und `T-0030` stand als oberster offener Punkt auf der Agenda — genau die „dafür vorgesehene Session", die B043 gefordert hatte; sie nicht zu nutzen wäre die sechste Wiederholung des Befunds gewesen, den das Ticket beschreibt. Umgesetzt: `frist` gilt für **jeden** Tickettyp und wird für jeden geprüft (bisher nur im `decision-request`-Zweig — ein Tippfehler in der Frist eines CR fiel lautlos auf „keine Frist" zurück); die Ampel-Regel liegt jetzt **einmal** in `board.frist_ampel` und wird von den offenen DRs **und** den Backlog-Fristen benutzt (sie stand inline in `aggregation.cockpit`); `board.ist_ueberfaellig` gilt nur für offene Tickets — eine gerissene Frist an einem erledigten Ticket ist Historie; die Cockpit-Kachel zeigt überfällige Tickets **vollständig und vor den Statuszahlen** samt „n Tage über" plus einen Zähler „n ohne Frist" für unterminierte Backlog-Tickets. Nebenbefund mitbehoben: `DATUM_MUSTER` prüfte nur die Form, „2026-13-01" kam durch und hätte über `frist_ampel` als **„grau" = keine Frist** gegolten — ein falsch terminiertes Ticket hätte wie ein unterminiertes ausgesehen; eine gemeinsame Prüfung `board.ist_datum` für `frist` und `erstellt`. **11 neue Tests, Gesamtsuite 329** (vorher 318), **Matrix 91 SWRs / 0 Lücken**, Katalog- und Architektur-Gate grün, Gegenprobe gegen den Altstand geführt (3 Tests scheitern dort nachweislich). **Eskalationsregel (die offene Frage aus T-0030), hiermit festgelegt:** Ein überfälliges Backlog-Ticket ist der **erste Arbeitspunkt der nächsten Routine-Session nach dem Briefkasten**, vor jeder neuen Fläche. Nimmt eine Session es trotzdem nicht auf, **schreibt sie den Grund in die Agenda** — nicht als Randnotiz, sondern beim Ticket. Ab dem **zweiten** übergangenen Mal geht ein Vermerk in den Briefkasten des Auftraggebers: Ab da hat das Team gezeigt, dass es sich selbst nicht korrigiert, und das ist seine Sache zu wissen, nicht unsere zu verwalten. **Sofort angewandt:** die drei offenen pm-Backlog-Tickets (`T-0028`, `T-0030`→`T-0032`, sowie die neu vergebenen Fristen) tragen jetzt Termine — ein Fristfeld, das nur die neuen Tickets nutzen, hätte den Befund nicht berührt | Frist als BOARD.md-Spalte vs. nur im Cockpit; Ampel für Backlog neu schreiben vs. die DR-Ampel zusammenführen; Eskalation automatisch erzwingen vs. Regel + Sichtbarkeit; Teil 2 (Uhrzeit-Takt) mitbauen vs. trennen | Eine neue Board-Spalte ist eine Formatänderung am generierten `BOARD.md` — genau die hat am 16.08. früh alle board-check-Workflows rot gemacht (`pm/T-0013`, Betriebs-Backlog); sie gehört gebündelt, nicht nebenbei. Eine zweite Ampel-Formel wäre die Falle aus B033 gewesen („welche Regel wäre ich versucht, hier noch einmal zu schreiben?"). Eine Automatik, die Prioritäten selbst umschreibt, hätte die Steuerung an ein Werkzeug abgegeben, das den Kontext einer Session nicht kennt — die Regel wirkt über Sichtbarkeit plus Begründungspflicht, und die Begründungspflicht ist das eigentliche Gegenmittel gegen das stille Liegenbleiben. Der Uhrzeit-Takt berührt F14 (Session-Takt, `p0/D027`) und die beiden bestehenden Taktlogiken — ihn im selben Zug zu bauen hätte eine dritte erzeugt, ohne dass die Abgrenzung geklärt ist | board.py, aggregation.py, app.js, test_board.py (7 Tests), test_org_cockpit.py (4 Tests), p9-Requirements v1.7 (SWR-091), pm/T-0030, pm/T-0032, pm/N-0025 |
| D006 | 2026-08-16 15:21 | Mensch (E. John, via Inbox) | **TG-a** | lt. T-0031 | — | T-0031 |
