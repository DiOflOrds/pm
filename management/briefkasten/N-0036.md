---
von: E. John
zeit: 2026-08-16T21:54:44+00:00
status: beantwortet
---

pm/T-0013: abschluss.cmd manuell ausgeführt

## Antwort des Teams (Session 2026-08-17 00:20)

**Kurz: danke — und ab jetzt musst du dafür nicht mehr auf die Actions-Seite schauen.**

Dass `abschluss.cmd` manuell durchgelaufen ist, sehen wir im Protokoll: der Lauf um **23:45** und
der um **00:00** endeten beide mit `OK - alles geprueft und gepusht`, der 00:00-Lauf hat
`pm 8a377d0..5a4b544` hochgeladen. Damit ist der lokale Teil von `pm/T-0013` bestätigt.

**Was noch fehlte, war der Blick auf GitHub — und der ist jetzt automatisiert.** Aus deiner Frage
vom selben Abend („welche Rechte muss ich geben?") ist `platform/T-0003` / **SWR-105** geworden:
Schritt **[5/5]** von `abschluss.cmd` öffnet keine fünf Browser-Tabs mehr, sondern fragt die
Actions-API ab und schreibt `CI-STATUS.md`. **Rechte braucht es dafür keine** — die Repos sind
öffentlich, die API antwortet ohne Anmeldung. Kein Token, nichts zu verwahren.

Die Regel, auf die es ankommt: geprüft wird der Lauf **zu genau dem Commit, der gerade gepusht
wurde**. Ein grüner Lauf eines anderen Commits zählt nicht, und „noch kein Lauf" wird als „noch
kein Lauf" gemeldet und nie als grün. Genau dieser Unterschied war der Grund, warum der Blick auf
die Seite bisher ein Mensch sein musste.

**Zwei Dinge, die du wissen musst:**

1. **`abschluss.cmd` ist neu geschrieben.** Beim Einbau von Schritt [5/5] hat die Session die Datei
   versehentlich geleert. Sie ist aus `abschluss-auto.log` und dem vorher gelesenen Push-Abschnitt
   **rekonstruiert**; jeder Schritt wurde einzeln gegen die echten Skripte nachgefahren und liefert
   dieselben Ausgaben wie im Protokoll (Matrix 104/0 und datakonv 18/0, Katalog und Architekturbild
   konsistent, 42 datakonv-Tests grün). Sie tut dasselbe, ist aber **nicht zeichengleich** mit dem
   Original. Wenn du eine Vorgängerversion hast (Explorer → Rechtsklick → *Vorgängerversionen*,
   OneDrive-Versionsverlauf, Backup), vergleiche sie bitte — im Zweifel gilt deine.
2. **Der erste Lauf am Host ist der eigentliche Nachweis für die CI-Prüfung.** In der
   Cowork-Sandbox gibt es keinen GitHub-Zugang; alle Tests injizieren die Abruffunktion. Belegt
   ist damit die *Auswertung*, nicht die *Abfrage*. Sagt `CI-STATUS.md` nach deinem nächsten Lauf
   denselben Commit und dasselbe Urteil wie die Actions-Seite, schließen wir `pm/T-0013`,
   `pm/T-0010` und `pm/T-0026` zusammen.
