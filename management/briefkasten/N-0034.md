---
von: E. John
zeit: 2026-08-16T21:53:14+00:00
status: beantwortet
---

pm/T-0034: Der Digest-Lauf braucht IMAP-Zugang und ein lokales Ollama. -> alles bereits schon vorhanden. Täglich und Manuelle Ollama kann man bereits ausführen. bitte die Zugangsdaten und ollama dafür übernehmen.

## Antwort des Teams (Session 2026-08-17 00:20)

**Kurz: die Zugangsdaten übernehmen wir nicht — und müssen es auch nicht. Sie liegen schon
richtig. Und der Befund aus `pm/T-0034` hat sich erledigt: der Wochendigest existiert.**

**1. Der Befund ist weg, und zwar belegbar.** `pm/T-0034` hieß „Wochendigest seit Teamgründung
fällig und nie erzeugt". Im Repo liegen jetzt beide:

* `team-mail/digest/2026-08-16-woche-digest.md` — 165 Mails, Commit `2e738f1`
  („Erster Wochendigest seit Teamgruendung").
* `team-mail/digest/2026-08-16-tag-digest.md` — **89 Mails, Commit `a1e9918` mit dem Vermerk
  „(Autopilot)"**, verdichtet mit lokalem Ollama (`gemma3:27b`).

Der zweite ist der eigentliche Beweis: Er sagt „Automatisch verdichtet (lokales Ollama, SWR-062)"
und **nicht** den Fallback-Satz, den die Session schreibt, wenn kein Ollama erreichbar war. Damit
ist belegt, dass IMAP **und** Ollama am Host laufen — genau das, was du schreibst. Das Ticket wird
geschlossen.

**2. Warum das Team keine Zugangsdaten „übernimmt".** Zwei Regeln stehen dagegen, und beide sind
älter als diese Frage:

* **Secrets nie in Git** (harte Regel). Alles, was das Team hat, liegt in Git — es hat schlicht
  keinen Ort, an dem ein Passwort sicher läge.
* `team-mail/konfiguration.yaml` sagt es in Zeile 2 selbst: *„NIE Passwoerter hier eintragen."*
  Das Feld heißt `env_suffix` und nicht `passwort`, weil die Konfiguration nur sagt, **welches**
  Konto gemeint ist — nicht, wie man hineinkommt.

**3. Wo sie stattdessen liegen — und wo sie offenbar schon liegen.** `mail_digest.py` liest zur
Laufzeit aus der Umgebung des Windows-Benutzers:

| Variable | Bedeutung |
|---|---|
| `MAIL_IMAP_HOST` | Server (Vorgabe `imap.gmail.com`) |
| `MAIL_IMAP_USER` | Konto — hier `dimitri.john83@gmail.com` |
| `MAIL_IMAP_PASS` | **App-Passwort**, nicht das Google-Kennwort |
| `OLLAMA_URL` | Vorgabe `http://localhost:11434` |

Dass der Autopilot-Digest von gestern existiert, heißt: **diese Variablen sind bereits gesetzt.**
Es ist nichts zu tun. Falls du sie prüfen willst: `setx` bzw. Systemsteuerung → Umgebungsvariablen
des Benutzers, nicht des Systems — dann sieht sie nur dein Konto.

**4. Was das Team stattdessen bekommt.** Es braucht die Geheimnisse nie zu sehen: Der Autopilot
läuft am Host unter deinem Benutzer, liest sie dort und legt **nur das Ergebnis** (den Digest) im
Repo ab. Das ist dieselbe Trennung wie beim Push — das Team schreibt Commits, hinausbefördert sie
der Mensch.

**Ein Hinweis, ungefragt, weil er im Digest von gestern steht:** Unter „Braucht Blick oder
Reaktion" stehen mehrere Google-Sicherheitswarnungen zu Kontoaktivität und ein PayPal-Beleg. Wenn
das App-Passwort neu ist, erklärt das die Warnungen — falls nicht, lohnt der Blick.
