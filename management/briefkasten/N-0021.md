---
von: E. John
zeit: 2026-08-16T08:11:46+00:00
status: beantwortet
---

beim pm sind noch ein paar tasks seit längerem in review, schau mal nach

---

## Antwort des Teams (Routine-Session, 2026-08-16)

**Du hast recht, und der Grund ist unangenehmer als „liegengeblieben".** Es sind zwei
Tickets — `pm/T-0010` (board-check-Flake) und `pm/T-0013` (Push-Reihenfolge: platform
zuerst) —, und beide stehen aus demselben Grund auf `in_review`: Sie warten laut Vermerk
darauf, dass ein **GitHub-Actions-Lauf nach dem Fix grün meldet**. Lokal ist beides in
Ordnung, der Nachweis sollte von außen kommen.

**Dieser Lauf konnte seit 09:44 nicht mehr stattfinden.** Der Auto-Push-Wächter ist seither
dreimal hintereinander abgebrochen (09:44, 09:59, 10:14) — zuletzt erfolgreich gepusht hat er
um **08:30**. Auf GitHub ist seitdem nichts angekommen: **21 Commits** und die beiden
Baseline-Tags **`p10-v1.0`** lagen nur lokal. Die Abnahme des zehnten Projekts, die du heute
um 10:02 per Knopf erteilt hast, war extern nicht vorhanden. Die zwei Tickets warteten also
nicht auf sich selbst, sondern auf ein Ereignis, das strukturell unmöglich geworden war.

**Ursache: ein Umlaut.** Der Preflight fragt vor dem Aufräumen von Git-Sperren nach, ob
gerade ein Git-Prozess läuft — auf Windows über `tasklist`. Dessen Ausgabe kommt in der
alten Konsolen-Codepage, Python las sie in einer anderen. Wenn **kein** git.exe läuft, lautet
die deutsche Antwort „…mit den angegebenen Kriterien **ausgeführt**." Das `ü` darin ist genau
das Byte, das beim Lesen platzt. Der Fehler fiel in einen vorsorglichen Auffang-Zweig, der
im Zweifel „es läuft ein Git" annimmt — die Abfrage meldete also **ausgerechnet dann
„besetzt", wenn nichts los war**. Ein zugehöriger Test wurde dadurch rot, `abschluss.cmd`
bricht bei rotem Test ab, und damit stand die ganze Kette.

Der zweite, gleichwertige Fehler steckte im Test selbst: Er prüft ein Wegwerf-Repo im
Temp-Ordner, fragte dafür aber den **gesamten Rechner** nach laufenden Git-Prozessen ab.
Ein Commit aus dem HMI oder der Wächter selbst hätten ihn ebenso rot gemacht. Beides ist
behoben (`pm/T-0024`), mit 4 neuen Tests und Gegenprobe gegen den alten Code — zwei davon
scheitern dort nachweislich, und der Abbruch von 10:14 ließ sich exakt nachstellen.

**Was uns daran mehr beschäftigt als der Bug:** Nicht die Automatik hat gemeldet, dass sie
steht — **du hast dich über zwei Tickets gewundert.** Der Wächter schreibt seine Fehlschläge
ausschließlich in `abschluss-auto.log`, und eine Logdatei liest ohne Anlass niemand. Nach
außen sah alles normal aus: Die Sessions liefen, committeten, meldeten STARTKLAR. Zwei
Stunden lang war der einzige sichtbare Hinweis auf einen stehenden Push, dass zwei Tickets
nicht weiterkamen. Wir haben das als eigenen Punkt vermerkt (CR-Kandidat: Warnmail nach dem
n-ten Fehlversuch des Wächters — die Mail-Strecke gibt es schon). Bewusst **nicht** nebenbei
gebaut, weil eine Mail Außenwirkung hat.

**Zum Status der beiden Tickets:** Sie bleiben `in_review` — jetzt aber mit realistischer
Aussicht. Sie einfach auf `done` zu setzen, wäre die bequeme Antwort auf deinen Brief und
genau der Fehler, den wir gestern als Lehre notiert haben: Ein Nachweis, der nicht geführt
wurde, wird nicht dadurch geführt, dass jemand nachfragt. Beide tragen jetzt einen Vermerk,
warum sie standen und woran man erkennt, dass sie fertig sind.

**Woran du siehst, dass es wieder läuft** (nächster Wächter-Lauf, alle 15 Min):
`PUSH-ANFORDERUNG.txt` im Wurzelordner verschwindet, in `abschluss-auto.log` steht
`OK - alles geprueft und gepusht`, und auf GitHub sind die Tags `p10-v1.0` da. Sobald die
board-check-Actions danach durchgelaufen sind, ist zugleich der Nachweis für `pm/T-0010`
und `pm/T-0013` erbracht — die gehen dann ohne weiteres Zutun auf `done`.

*Verbucht als `pm/T-0024` (SUP.9). Vermerke in `pm/T-0010` und `pm/T-0013`.*
