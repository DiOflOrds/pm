---
von: E. John
zeit: 2026-08-16T12:05:01+00:00
status: beantwortet
---

Quelle: 1-4000 Zeichen (Zeilenumbrüche werden automatisch zu Leerzeichen). reicht auch nicht aus

---

## Antwort des Teams (Routine-Session, 2026-08-16 14:05)

**Erledigt — und diesmal keine neue Zahl geraten.** `pm/T-0027` (Antwort auf `N-0023`) hatte
die Grenze von 200 auf 4000 Zeichen angehoben; das reicht für ein „Quelle"-Feld mit einem
längeren Herkunftsbeleg offenbar immer noch nicht. Zweiter Fehlversuch, eine konkrete Zahl
zu raten — also diesmal keine dritte Zahl, sondern die eigentliche Ursache behoben: Die
Obergrenze war nie ein inhaltliches Limit, sondern nur der Schutz der
Markdown-Tabellenzeile, und dafür ist — wie in `T-0027` schon festgehalten — einzig `|`
das Zeichen, das wirklich etwas sprengt.

- **`FELD_MAX`: 4000 → 200 000 Zeichen** (`platform/backend/pool.py`) — keine Inhaltsgrenze
  mehr, nur noch eine technische Notbremse gegen einen versehentlichen Mega-Paste. Gilt für
  alle Felder, die durch dieselbe Prüfung laufen (Kurzbeschreibung, Kandidat-Text, Nutzen,
  Voraussetzung, Quelle) — nicht nur für „Quelle", sonst wäre in zwei Wochen derselbe Brief
  zu „Nutzen" fällig.
- Im Pool-Reiter sind „Nutzen"/„Voraussetzung"/„Quelle" jetzt ebenfalls Mehrzeilenfelder
  statt einzeiliger Eingaben — reine Darstellung, die Prüfung bleibt serverseitig.
- Weiterhin hart verboten bleibt nur `|` (sprengt die Tabellenzeile).

**Nicht mit angefasst:** Der „Starten"-Knopf bleibt unverändert (Bestandsverhalten aus
`pm/T-0022`, kein Teil dieses Befunds — siehe `T-0027`).

**Verifikation:** 1 neuer Regressionstest (bei der alten 4000er-Grenze hätte er abgelehnt),
Gesamtsuite **310 Tests** (vorher 309), alle grün. Matrix unverändert 89 SWRs / 0 Lücken,
Katalog- und Architektur-Gate geprüft und grün. Kein neuer SWR — Korrektur von SWR-088.

**Deine Stichprobe (Server vorher neu starten):** Pool-Reiter → „Neuen Kandidaten
anlegen" → Technik-Kandidat, in „Quelle" einen deutlich über 4000 Zeichen langen Text
eintragen → anlegen → erscheint als eine Zeile im Pool, keine Ablehnung.

*Verbucht als `pm/T-0029` (SUP.9, Korrektur von SWR-088 — kein neuer SWR, Klasse C: rein
technische Validierungsgrenze ohne Geld-/Rechts-/Außenwirkung).*
